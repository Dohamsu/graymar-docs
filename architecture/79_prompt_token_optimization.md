# 79. 프롬프트 토큰 최적화 — 측정 기반 스테이지별 예산 (2026-07-18)

> **상태**: ✅ **P3~P4 완료** (2026-07-19) — D 재시도 스킵(16.5%→0%) · A 시스템 -62%(12,154→4,668자) · B NPC 클러스터 지시 압축 · C 총량 백스톱 16,000자. **최종: avg 7,495tok(-31%), 12k+ 절벽 턴 0%, 게이트 7런 전부 10/10, 회귀 0, devotee 관계 누적 정상.** 대화 턴 대명사 기저(~29%)는 크기 무관 별개 문제로 확정(§9.1 교란 재해석) — arch/78 2차 처방 백로그 이관. 진행 로그 §9가 정본. 잔여: intent A/B·캐시 배치(후순위).
> **경과**: P1+정밀 스터디(1,556턴 — hard/soft 지시 두 계급, 11k 절벽) → 보고서 playtest-reports/prompt_compliance_study_20260718.md → P2 예산 10k → P3 4파트 실행.
> **동기**: 저렴한 모델(Gemma 4 26B **a4b** MoE — 활성 4B급 / gpt-4.1-nano) 운영 전제에서, 프롬프트가 길수록 지시 준수율이 떨어지는 한계를 **측정으로 정량화**하고, "지시를 잘 따르는 토큰량"을 스테이지별 예산으로 확정해 각각 최적화한다.
> **관련**: [[architecture/11_llm_prompt_caching|llm prompt caching]](설계만), arch/52(시스템 프롬프트 압축 -21%), 프롬프트 최적화 v2(선별 주입), arch/62(레이턴시), CLAUDE.md LLM 설계 원칙 6(프롬프트 최소주의).

## 1. P0 — 현황 실측 (2026-07-18, llm_call_logs 최근 3일 · 12턴×5런 포함)

| stage | 모델 | 호출 | avg 프롬프트 tok | max | avg 출력 | avg ms | 비용 비중 |
|---|---|---|---|---|---|---|---|
| **narrative** (메인 서술) | Gemma 4 26B | 1,588 | **10,856** | **15,429** | 220 | 4,418 | **61%** ($1.71) |
| narrative-retry | Gemma 4 26B | 262 | 10,085 | 14,469 | 192 | 4,857 | 7% |
| nano-event-director | 4.1-nano | 1,627 | 1,571 | 2,356 | 218 | 2,581 | 13% |
| npc-reaction | 4.1-nano | 832 | **2,439** | 3,061 | 172 | 2,330 | 8% |
| **intent** (LlmIntentParser) | 4.1-nano | 646 | **3,856** | 4,070 | **37** | 1,272 | 5% |
| challenge-classifier | 4.1-nano | 787 | 742 | 883 | 57 | 1,430 | 3% |
| news-article / mid-summary / nano-director / dialogue / marker-* / fact-extract | nano·Gemma | 소량 | 231~913 | — | — | — | 3% |

### 발견 (실측 근거)

1. **메인 서술 프롬프트 = 명목 예산의 4.3배**. 불변식 17 "Token Budget 2500"은 실제로 **memoryParts 블록(assistant role 메모리)만** 트리밍한다 (prompt-builder L241~280). 시스템 프롬프트·facts 블록·디렉티브·세션 턴·NPC 블록은 무예산 — 총합 avg 10.9k / max 15.4k. 활성 4B급 모델에 15k 컨텍스트는 지시 희석이 유력한 구간.
2. **재시도율 16.5%** (narrative-retry 262/1,588) — 원인 미분류 (타임아웃/빈 응답/검증 실패). 재시도도 같은 10k 프롬프트로 나가 비용·레이턴시 이중 지불.
3. **intent 파서 비대**: 출력 37토큰(JSON 분류 1건)에 프롬프트 3,856토큰 — 시스템 프롬프트 단독 5,197자(예시 다수). 646호출(매 LOCATION 턴). 분류 정확도 대비 예시 과잉 여부 미측정.
4. **npc-reaction 2.4k** — nano 대상 프롬프트 중 최대. JSON 실패 재시도 이력 있던 스테이지(arch/67 — 실패 10.4%→재시도 구제).
5. **이상 신호**: narrative가 gpt-4.1-**nano**로 24건 실행됨 (fallback 정본은 4.1-mini) — 경로 미확인.
6. **캐시 미활용 의심**: cachedTokens 컬럼은 있으나 활용 집계 미확인. 시스템 프롬프트+정적 블록은 매 턴 동일한데 캐싱 설계(arch/11)는 미구현.

## 2. 목표

- **각 스테이지의 "준수율 유지 최대 토큰량"을 실측으로 결정**하고 스테이지별 예산으로 고정 (감이 아닌 곡선의 무릎에서 결정).
- 결과적으로: 메인 서술 프롬프트 총량 유예산화, 재시도율 하락, 턴 비용·레이턴시 절감 (부수 효과이지 1차 목표 아님 — 1차 목표는 **지시 준수율**).

## 3. 단계 계획

### P1 — 준수율 메트릭 정의 + 회고 상관 분석 (코드 0줄로 시작)

기존 자산으로 "프롬프트 크기 ↔ 지시 준수" 상관을 **이미 쌓인 데이터에서** 뽑는다:

- 데이터 조인: `llm_call_logs.calls[].promptTokens` × `turns.llm_prompt`(프롬프트 원문 저장됨) × 턴 서술 원문.
- **스테이지별 준수율 지표** (전부 기존 검증기 재사용):
  | stage | 준수 지표 (기존 자산) |
  |---|---|
  | narrative | 어체 준수율(llm_speech_audit), @마커 정합(V8), 금지어/메타 서술(audit_quality), 개시어율(D5-1), questReveal 반영, [최근 사용 표현] 준수 |
  | intent | actionType 정답률 (KW_OVERRIDE·수동 라벨 표본 ~100건) |
  | npc-reaction / nano-event | JSON 파싱 성공률·필드 유효율·재시도 발생률 (`attempts`, 서버 로그) |
  | dialogue | 어체 검증 통과율 (validateSpeechRegister 1차 통과 기준) |
- 산출: 스테이지별 **프롬프트 토큰 구간(2k 단위 버킷) × 위반율** 곡선. narrative는 표본 1,588건으로 즉시 가능.
- 재시도 원인 분류 (P0-2) + narrative@nano 24건 경로 규명 (P0-5).

### P2 — 예산 결정 (측정 → 숫자 고정)

- P1 곡선에서 위반율이 꺾이는 지점을 스테이지별 예산으로 채택. 곡선이 평평하면(크기 무상관) "현 크기 유지 + 비용만 절감" 모드로 전환 — **억지 삭감 금지** (규칙 희석 역효과, LLM 설계 원칙 6).
- 예산 후보 초안 (P1 결과로 대체될 가설값): narrative 총량 6~8k / intent 1.5k / npc-reaction 1.5k / nano-event 1.2k.
- 예산 상수는 `token-budget.service.ts`에 스테이지별로 외부화 (불변식 30 정신 — 하드코딩 금지).

### P3 — 스테이지별 최적화 실행 (우선순위 = 비용×준수 영향)

1. **narrative**: enforceTotal 스코프를 memoryParts → **전 블록**으로 확장 (시스템 프롬프트·고정 디렉티브는 보호 대상 명시, L0 테마 불변식 5 준수). 블록별 실측 기여도(P1에서 블록 단위 토큰 분해) 기반으로 LOW/HIGH 우선순위 재조정. 세션 턴 6개+THREAD 하이브리드의 중복 주입 여부 점검.
2. **intent**: 예시 축소 A/B — 정답률 표본(P1)으로 예시 절반/카테고리당 2개 버전 검증. 정확도 하락 시 원복 (정확도 > 토큰).
3. **npc-reaction / nano-event**: 주입 블록 선별 강화 (등장 NPC 외 정보 제거 — 선별 주입 불변식 24 재적용 점검).
4. **캐싱 (선택, P3b)**: OpenRouter/OpenAI prompt caching — 시스템 프롬프트+정적 블록을 메시지 선두 고정 배치로 캐시 적중 확보 (arch/11 설계 소생). cachedTokens 집계로 적중률 검증. 예산화와 독립적이라 병행 가능.
5. **재시도 경로**: P1 원인 분류 결과에 따라 — 검증 실패형이면 프롬프트 수정, 타임아웃형이면 arch/62 레버.

### P4 — 검증 게이트

- 각 최적화 단위마다: 유닛 green + **12턴×2런** (D5·V1~V10 전문항 + P1 준수율 지표 재측정) — 준수율 **비악화**가 통과 조건, 토큰·비용·레이턴시 절감은 보고 지표.
- 최종: 12턴×3런 누적 + NPA 감사 1회. 성공 기준 = ① 스테이지별 예산 내 p95 수렴 ② 준수율 전 지표 비악화 ③ 재시도율 16.5% → 10% 이하.

## 4. 비범위 (이번 트랙에서 하지 않음)

- 모델 교체/평가 (arch/25 별도 트랙), 스트리밍 구조 변경 (금지선 — arch/77 P4.0), JSON 모드 전환(스트리밍 비호환), 시스템 프롬프트 **내용** 재작성(압축은 arch/52에서 기완료 — 이번엔 배치·예산·주입 선별만).

## 5. 리스크

- **트리밍 = 동작 변경**: 블록이 잘리면 서술 품질이 미묘하게 변한다. 반드시 P4 게이트로 스텝별 확인 (arch/77 방식).
- 캐싱은 provider 의존 (OpenRouter 경유 Gemma 캐시 지원 여부 확인 선행).
- intent 예시 축소는 KW_OVERRIDE 안전망이 있으나 고위험 오분류(FIGHT 미탐 등)는 게이트에서 별도 확인.

## 6. P1 결과 (2026-07-18 — 최근 3일 108런 1,556턴 회고, audit_quality 정본 재사용)

### 6.1 토큰량 × 준수율 상관

| 지표 | 결과 |
|---|---|
| hard 규칙 위반 (audit real — 금지어·서술체·마커 구조) | **토큰과 무상관** (r=-0.012, 위반턴율 전 구간 0~1.6%) |
| soft 문체 지시 (대명사 개시어율, D5-1) | **강상관 r=0.715** — 8~9k 구간 3.3% → 12k+ 구간 26.7% |

**교란 통제**: 토큰×턴번호 r=0.315의 교란 가능성을 턴 밴드(T1-6/T7-12/T13+) 층화로 분리 — **모든 밴드에서 <10k는 3~5%, 12k+는 24~29%** (T1-6에서조차 3.2% vs 27.3%, 8.5배). 세션 후반부 효과가 아니라 **프롬프트 크기가 원인**.

**무릎 = 10k**: <10k에서 균일하게 3~5%, 10~12k에서 8~23%로 전이, 12k+에서 붕괴.

> **arch/78 해석 정정**: "-20% 잔여 = soft 지시 천장"이 아니라 **12k+ 비대 턴들이 평균을 끌어올린 것**. <10k 턴에서는 지시가 잘 먹힌다(3~5%). 프롬프트 다이어트가 곧 대명사 잔여의 근본 처방.

### 6.2 재시도(ShortResponse) — 역상관·낭비 확인

- 발화 지점: llm-worker "응답 <200토큰이면 재시도" (Flash Lite 시절 방어). 현재 alternate 모델 = 메인과 동일(Gemma 4)이라 같은 모델·같은 프롬프트 재호출.
- 버킷별 재시도율: **<10k 26.3%** / 10-12k 18.9% / 12k+ 3.9% — 작은 프롬프트(단순 장면)의 정상적 짧은 서술을 실패로 오판.
- 재시도 평균 출력 192토큰(<200) — 재시도도 대부분 짧아 폐기. 이중 지불만 남음.

### 6.3 블록 분해 (13k+ 표본)

시스템 프롬프트 **12,154자(전체 55%)** — "P2 권장 규칙" 섹션 11,089자. **arch/52 압축(-21%, ~9,000자) 이후 재비대 확인**. 동적 블록(assistant 메모리 3.2k자 — 기존 예산 관할)은 오히려 작음. facts/디렉티브(user) 6.8k자.

### 6.4 기타

- **캐싱 이미 부분 작동**: narrative avg cached 5,350/10,805 (~50%), intent 59% — provider 암묵 캐싱. 비용 축은 양호, 문제는 길이에 따른 지시 희석 (캐시로 해결 불가).
- **narrative@nano 24건**: 전부 2026-07-16 오전 실험 런(T5~8 페어), 이후 재발 0 — 런타임 설정 실험 흔적 판정, 조치 불요.
- nano 스테이지 준수율(JSON 유효율)은 회고 데이터 부족 — P4 게이트에서 측정.

## 7. P2 예산 확정 + P3 우선순위 재조정 (P1 근거)

**스테이지 예산**: narrative **총량 ≤10,000 tok (p95)** — 곡선 무릎 채택. nano류는 위반 무상관이므로 예산 강제 없이 비용 관점만 (intent 3.9k는 P3 후순위).

**P3 재조정 (효과순)**:
1. **P3-1 시스템 프롬프트 재비대 정리 + 동적 총량 예산**: 시스템 12.2k자 재압축(arch/52 2차) + enforceTotal 전 블록 확장으로 총량 10k 상한. 대명사 잔여의 근본 처방 겸용 — 층화 데이터상 <10k 달성 시 대명사율 3~5% 기대 (arch/78 목표 -30%를 크게 상회).
2. **P3-2 ShortResponse 재시도 수정**: alternate==메인 모델이면 스킵(동일 모델 재호출은 무의미) — 소형 턴 26% 이중 지불 제거.
3. P3-3 intent 예시 축소 A/B (비용만, 정확도 우선).
4. P3-4 캐시 적중률 향상(블록 배치 안정화) — 선택.

## 8. 파트별 조립 최적화 계획 (2026-07-18 정밀 스터디 반영 — 정본)

> 근거 전체: `playtest-reports/prompt_compliance_study_20260718.md` (지표 M1~M10 × 6버킷 + 블록 성장 프로필).

**정밀 스터디 추가 발견**:
- 지시는 두 계급 — **구조 규칙(경어체 금지·1인 응답·금지어)은 15k에도 0% 위반으로 강건**, **문체·귀속 규칙은 11k 절벽**: 개시어 "최대 1개" 직접 위반 2.8%(9-10k)→**51.8%**(13k+), 무마커 대사 0%→9.1%, 대명사 개시 6.6%(10-11k)→20.5%(11-12k) 3.1배 점프.
- 12k+를 만드는 건 **대화 턴의 NPC 발화 가이드 5블록 클러스터** ([NPC 대화 자세] 1,062자 + [집중 NPC 반응 지시] 798 + [NPC 감정 상태] 505 + [이번 턴 NPC 말투] 240 + [NPC 이번 턴 톤 가이드] 338 ≈ 2,900자) + 시스템 바닥 12.2k자. NPC 연기용 블록들이 프롬프트를 절벽으로 밀어 문체·대사 귀속을 무너뜨리는 아이러니.
- ShortResponse 재시도 피크는 9-10k 31.3% — 단순 장면의 정상 짧은 서술 오판.

| 파트 | 처방 | 목표 | 순서 |
|---|---|---|---|
| D. ShortResponse 재시도 | alternate==메인이면 스킵 | 재시도율 16.5%→~3% | 1 (무위험) |
| A. 시스템 프롬프트 | 재압축 2차 (P2 권장규칙 11,089자 위주 — 중복·장황 제거, 내용 보존) | 12,154자→≤9,000자 (-25%) | 2 (정적) |
| B. NPC 발화 가이드 5블록 | [이번 턴 NPC 연기] 1블록 통합 — NPC당 자세·반응·감정·말투·톤 1섹션 | ~2,900자→≤1,200자 (-60%) | 3 (통합 재작성) |
| C. 총량 예산 | enforceTotal 전 블록 확장, 상한 10k (보호: 시스템·P0·이번 턴 행동·판정) | 13k+ 턴 비중 41%→<10% | 4 (잔여 강제) |

각 파트 독립 커밋 + 게이트(12턴×2런 — M1·M2·M5 재측정 + V1~V10 + D5), arch/77 방식. **성공 기준**: M1 전 구간 3~6% · M2 위반 <10% · M5 <3% · 재시도율 <10% · hard 규칙 비악화.

### 8.1 부작용 검토 — 플레이 컨셉("플레이어가 만드는 세계") 보존 (2026-07-19 소유자 확인)

- **구조적 안전**: 플레이어가 만든 세계의 실체는 DB runState(worldFacts·NPC 감정·개인 기억·퀘스트, 불변식 1)이며 본 계획은 그 데이터를 삭제하지 않는다 — 바꾸는 건 "한 턴의 서술자 브리핑 분량"뿐 (불변식 2: 게임 결과 무영향). 역실측: 12k+ 턴은 무마커 대사 9.1%·지시 위반 51.8%로 **현상 유지가 오히려 살아있는 세계의 렌더링을 훼손**.
- 파트별 위험: ① 없음 / ② 낮음 — 규칙 압축 시 컨셉 보호 규칙(의도 존중·미소개 실명 차단) 문면 보존 검증 / ③ 조건부 — **데이터 무손실·형식만 통합** 원칙 (감정·반응·말투 정보 보존, 5중 헤더·중복 지시문만 제거) / ④ **최대 위험 지점** — 기억 블록이 잘리면 "NPC가 이력을 잊음" = 최대 강점(장기 기억) 직격.
- **④ 안전장치**: 보호 목록에 기억·연속성 블록 전체(L0 테마 불변식 5 절대 불가 + NPC 기록·방문 대화·직전 발언·questReveal 최후순위) — 트리밍은 중복 지시문 먼저, 플레이어 이력 마지막. ④는 백스톱(예산 달성은 ②+③으로, ④는 예외 턴만). 게이트에 devotee·coercer 관계 누적 런으로 "NPC가 내 이력을 기억하는가" 직접 확인.

## 9. 진행 로그 (P3 실행 — 2026-07-18~19)

- **P3-D ShortResponse 재시도 스킵** (server adc2282): alternate==메인 모델이면 재시도 스킵 (provider 인지 비교). 게이트 12턴 10/10 — 짧은 응답 7건 발생·재시도 0건 실측. 원인 규명: "출력<200tok=실패" 휴리스틱이 단순 장면의 정상 짧은 서술을 오판(Flash Lite 시절 방어의 잔재), 재시도 평균 출력 192tok로 대부분 폐기되던 이중 지불 제거.
- **P3-A 시스템 프롬프트 재압축** (server e53edb9): 12,154자 → **4,668자 (-62%)** — P0/P1/P2 우선순위 박스를 정본으로 승격, 본문의 1:1 중복 재서술 제거. 고유 규칙 11종 문면 잔존 자동 검증(해라체/speechRegister/골드/환상 재해석/선제 대화/분사구/자기 일관성/대사 한 덩어리/홑따옴표/주제 반복/퇴장). 스냅샷 17건 의도 갱신. 게이트 chatty/brawler 각 10/10. **효과: narrative 프롬프트 avg 10,856 → 7,5~8,1k tok (-26~31%), max 15.4k → 11.0k.** 교훈: TS 템플릿 리터럴 안 백틱 금지(문자열 조기 종료로 빌드 실패 실측).
- **P3-B NPC 클러스터 지시문 압축** (server 1b6694f): [NPC 대화 자세] 6줄→2줄 · NpcReaction 위반 사례 6줄→1줄 · 톤 가이드 3줄→1줄 · [집중 NPC 반응 지시] 3줄→1줄 — **데이터 필드 무손실**. "그대=마이렐 전용" 콘텐츠 리터럴을 "타 NPC 고유 호칭 차용 금지"로 일반화(불변식 45 정합). **계획 편차: "1블록 통합" 대신 소스 레벨 압축** — 동일 목표(중복 지시 제거) 달성, 5개 데이터 소스의 파이프라인 병합이라는 구조 리스크 회피. 게이트 2런 10/10.
- **P3-C 총량 백스톱** (GRAND_TOTAL_CHAR_BUDGET=16,000자 ≈ 9.7k tok, token-budget 외부화): 전 메시지 합산 초과 시 스냅샷성 블록([NPC 일상]/[세계 상태]) 통째 제거 → 기억성 블록([관련 사건/NPC 기록]) 뒤에서 부분 절삭(완전 삭제 금지) → 잔존 초과는 보호 블록 유지+경고 로그. 유닛 4케이스. 게이트 1런 10/10, max 10,120tok·10k+ 1턴·GrandBudget 발화 14회. 구형 fixture 스냅샷 5건 의도 갱신 (diff 검증: TIER 블록만 제거, [주인공 배경] 보존 확인).

### 9.1 인과 재해석 — 대화 턴 교란 발견 (2026-07-19, P3-C 게이트 중)

P3-A/B로 평균이 7.5~8k로 내려왔는데도 대명사 개시어율이 19~22%로 잔존 → §6.1 층화를 **턴 성격(대화=NPC 대화 자세 블록 존재) 기준으로 재층화**:

| 턴 성격 | <10k | 10-12k | 12k+ |
|---|---|---|---|
| 대화 턴 | **표본 0** (구 구조에선 대화 턴이 항상 10k+) | 21.8% | 27.0% |
| 비대화 턴 | 3.9% | 11.5% | 23.5% |

- **크기 효과는 실재** (비대화 3.9%→23.5%) — 다이어트·상한의 근거 유효.
- 그러나 **대화 턴은 기저 대명사율 자체가 ~22%** (지칭 대상 존재) — §6.1의 "<10k면 3~5%"는 비대화 턴에만 성립하던 약속. 구 데이터에서 크기가 대화 턴 여부를 완전 대리(proxy)해 교란됐다.
- 결론: 성공 기준 "M1 전 구간 3~6%"는 **비대화 턴 한정으로 정정**. 대화 턴 대명사 습관은 크기가 아닌 지칭 스타일 문제 — arch/78 2차 처방(디렉티브 승격/후처리) 백로그로 회귀.

### 9.2 P4 최종 판정 (2026-07-19 — 최종 코드 3런 42턴: chatty/brawler/devotee 15턴)

| 지표 | 성공 기준 | 결과 | 판정 |
|---|---|---|---|
| 12k+ 턴 비중 (절벽 구간) | <10% | **0%** (10k+도 5%, max 10,226) | ✅ |
| 평균 프롬프트 | ≤10k (p95) | **7,495 tok** (baseline 10,856 대비 -31%) | ✅ |
| 재시도율 | <10% | **0%** (16.5%→0) | ✅ |
| hard 규칙·게이트 | 비악화 | V1~V10 **7런 전부 10/10 PASS**, 회귀 0 | ✅ |
| 컨셉 보존 | NPC 이력 기억 | devotee 15턴: 하를런 trust 36 누적·위화감 노트 0·V5 PASS | ✅ |
| M1 비대화 턴 | 3~6% | 8.2% (146문장 소표본) | ⚠️ 근접 |
| M1 대화 턴 | — (§9.1 기준 정정) | 29.3% — 크기 무관 기저 수준(27%) 재확인 | 기준 외 |
| M2/M5 | <10% / <3% | 72.2% / 10.3% — **전후 비교 불가 판정** | 무효 지표 |

- **M2 비교 불가 사유**: arch/78 임계 3→2 + 대명사 합산으로 [최근 사용 표현] 지시가 상시 발화되고 banned 목록에 대명사가 항상 포함되게 됨 — 대화 턴 기저 29%에서 "해당 개시 문장 최대 1개"는 구조적으로 자주 초과. 구 데이터의 M2(임계 3, 대명사 미합산)와 분모·조건이 다른 지표가 됐다. soft 지시의 준수 한계(arch/78 -20% 관찰)의 재확인이지 이번 트랙의 회귀가 아님.
- **M5**: 6/58 (V8 마커 정합 게이트는 전 런 PASS — 8자+ 인용 중 비대사 인용 포함 소음, 소표본).

**종합: 목표(예산·재시도·비용) 완전 달성, 회귀 0.** 프롬프트 다이어트는 비대화 턴 문체를 안정권(8.2%)에 두었고, 대화 턴 대명사 기저(~29%)는 크기와 무관한 별개 문제로 확정 — arch/78 2차 처방(지칭 스타일: 디렉티브 승격/후처리) 백로그로 최종 이관. 비용 부수효과: narrative 프롬프트 토큰 -31% ≈ 턴당 비용·캐시 부담 동반 감소.

**잔여 백로그**: ① intent 예시 축소 A/B (P3-3 — 비용만) ② 캐시 배치 안정화 (P3-4) ③ 대화 턴 대명사 2차 처방 (arch/78 §5-1) ④ M5 무마커 대사 6건 원문 검토 (소액).
