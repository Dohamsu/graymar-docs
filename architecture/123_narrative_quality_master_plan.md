# 123. 대사·서사 품질 개선 마스터 계획 (2026-09-25)

> 상태: **P1~P6 부분 구현 / 두 팩 단기 실런 진행, 출시 관문 미통과** (2026-09-26). 근거: [[architecture/122_narrative_reader_experience_observations|독자 경험 관찰 기록]].
> 범위: 저작 팩 `graymar_v1`·`star_sand_v1`의 일반 독자 경험. 판정·월드 상태는 서버 정본, LLM은 그 결과를 표현한다는 기존 원칙을 유지한다.
> 목표: 문장만 윤문하는 프로젝트가 아니다. **행동 → 사건·단서·관계 변화 → 그 변화를 반영한 대사와 다음 선택 → 누적된 결말**을 독자가 끊김 없이 따라갈 수 있게 한다.

## 0. 완료의 정의와 변경 원칙

처음 읽는 사람이 매 장면 뒤에 다음 네 질문에 답할 수 있어야 한다. **지금 어디에 있는가? 방금 무엇을 시도했고 실제로 무엇이 일어났는가? 새로 알거나 잃은 것은 무엇인가? 그래서 다음에 할 수 있는 일은 무엇인가?** 이 네 질문의 답을 오직 내부 상태나 로그가 아니라 화면에 나온 본문·선택지에서 찾을 수 있어야 한다. 의도적인 비밀·실패·NPC의 거절은 허용하지만, 그 사실 자체와 이유·후속 방향은 읽혀야 한다.

계획을 실행할 때 지킬 경계:

1. **새 기능보다 정합 우선.** 기존 `EventChoiceGate`, `QuestForwardChoice`, `EmptyHandedHint`, 대화 잠금, 아크 3막(arch/98·103·104·110)을 먼저 추적한다. 이미 있는 장치를 복제하거나 규칙 문장만 추가하지 않는다.
2. **서버 사실을 서술이 결정하지 않는다.** `locationId`, 행동 판정, NPC 정체, 퀘스트 사실, 비용·보상, 엔딩 루트는 서버/콘텐츠 정본이다. 서술에서 새 수치·증거·이동·거래를 꾸며 상태에 역수입하지 않는다.
3. **확정 결함과 감상·가설을 분리한다.** N-10/11/13의 실제 표면 어긋남은 우선 다루되, N-12/14/16은 과거 표본 또는 후속 코드 변경이 있으므로 현행 재현을 먼저 확인한다. 재현되지 않으면 예방 테스트만 남기고 수정 범위를 늘리지 않는다.
4. **모델 호출 수 증가를 기본 해법으로 삼지 않는다.** 선택지·대사 연결에 필요한 서버 정본 데이터와 이미 생성된 결과를 먼저 활용한다. 추가 JEV/nano/메인 호출은 정확도·지연·운영 복잡성의 실측 이득을 확인한 뒤 별도 결정한다.
5. **기존 장점을 보존한다.** N-01의 사건형 오프닝, 장면 고유 사물을 이용한 선택지, NPC별 말투와 팩별 분위기, 자유 탐험을 없애지 않는다. ‘모든 턴에 강제 사건’이나 ‘모든 NPC가 즉시 정답 공개’로 해결하지 않는다.

## 1. N-01~N-21 분류와 담당 단계

| 분류 | 항목 | 독자에게 보이는 증상 | 우선 담당 | 현재 증거 수준 |
|---|---|---|---|---|
| 보존 기준 | N-01 | 강한 시작 장면·사건 약속 | 전 단계 회귀 기준 | 최근 본문+정본 |
| 도입·동선 | N-02 | 그레이마르의 개인적 대가가 희미함 | P4 | 정본/프롤로그 비교·해석 |
| 도입·동선 | N-03 | 의뢰 직후 같은 여관 재진입이 제자리걸음처럼 보임 | P4 | 최근 두 경로 |
| 도입·동선 | N-04 | 이동 턴은 길지만 사건 변화는 뒤로 밀림 | P4 | 최근 여러 이동 턴 |
| 선택·진전 | N-05 | 장면 고유 선택지 뒤 범용 메뉴 반복 | P3 | 최근 네 런 반복 |
| 선택·진전 | N-06 | 새 단서 직후 다시 암시만 하는 대화 | P3 | 최근 한 연속 대화 |
| 선택·진전 | N-07 | 말투는 다르나 정보 회피·인계 패턴은 비슷함 | P3 | 최근 두 팩 정독·해석 |
| 선택·진전 | N-08 | 숫자는 많은데 무엇이 증거인지 불분명 | P3 (P2 정본 계약 참조) | 최근 두 런 정독·해석 |
| 문장 리듬 | N-09 | 냄새·시선·손끝이 행동보다 먼저 반복됨 | P6 | 최근 네 런 정독+단순 집계 |
| 장면 정합 | N-10 | 회계사 장면에 악사 선택지가 노출됨 | P1 | 최근 단일 사례, `choiceSurface=server` 확인 |
| 장면 정합 | N-11 | 경비대 입구→외곽 술집의 전환 다리가 없음 | P1 | 최근 단일 사례, 저작 이벤트 확인 |
| 행동 정합 | N-12 | “싸움을 건다”가 장부 설명에 묻힘 | P1 조건부 | 9/21 과거 회귀 후보 |
| 사실 정합 | N-13 | 이렌의 ‘못 잠’/‘못 깨어남’ 충돌 | P2 | 정본+최근 두 런 교차 확인 |
| 대사 표면 | N-14 | 무표기 발화·따옴표 끊김 | P2 조건부 | 화자 후처리 수정 전 표본 |
| 문장 표면 | N-15 | `faint하게` 등 언어·문장 오류 | P6 | 최근 개별 사례 |
| 결말 | N-16 | 수집한 증거·가격·결정의 대가가 결말에 약함 | P5 조건부 | 9/2 과거 완주 후보 |
| 선택·진전 | N-17 | 새 단서가 가리키지 않은 장소로 즉시 이동 선택지가 뜸 | P3 | 9/25 극야해안 실런 재현·수정 후 재검증 |
| 문장 표면 | N-18 | 이동 서술 대신 JSON형 계획 문구가 본문에 노출됨 | P6 긴급 회귀 | 9/25 극야해안 실런 재현·검증 중 |
| 선택지 정합 | N-19 | 생성 선택지에 세계관 밖 화폐 `크레딧`이 노출됨 | P2 | 9/26 그레이마르 10턴 실런 재현·단위 방어 |
| 종결 안정성 | N-20 | 엔딩 조건 충족 턴의 서버 500으로 완료되지 않음 | P5 | 9/26 그레이마르 장기 런 재현·코드 수정 후 종료 실런 확인 |
| 종결 선택권 | N-21 | 커밋한 아크의 3막 뒤 `arc_finale`를 누르기 전에 자동 종료됨 | P5 | 9/26 목적 런 재현·수정 후 명시 선택 실런 확인 |

P0은 전 항목의 현행 상태를 확인하는 관문이다. P7은 전 단계 통합 검증이다. 표의 한 항목을 여러 단계가 참조할 수 있지만, **완료 책임은 ‘우선 담당’ 한 단계에만** 둔다.

## 2. 실행 순서와 의존 관계

```text
P0 현행 기준선·재현
  → P1 장면/행동/선택지 정합
  → P2 정본 사실/대사 표면 정합
  → P3 대화·단서·후속 선택의 진전
  → P4 첫인상·이동 리듬
  → P5 결말의 선택·대가·파급 효과
  → P6 문장 리듬과 표면 마감
  → P7 두 팩 통합 검증·출시 판단
```

이 순서의 이유는 간단하다. 엉뚱한 선택지나 장소가 나오는 상태에서 대사만 개선하면 그 문장도 독자가 믿지 못한다(P1). 사실의 정본이 흔들리면 대화 진전을 설계할 수 없다(P2). 단서와 다음 행동의 연결이 생긴 다음에야 첫 10턴의 길이·이동을 줄여도 정보 손실을 판단할 수 있다(P3→P4). 결말은 그 앞에서 축적한 선택을 소비하므로 뒤에 검증한다(P5). 문장 취향 문제는 구조 변경이 끝난 뒤 마지막에 다뤄 재작업을 줄인다(P6). **이는 완료·출시 검증의 의존 순서이며, 독립적인 표적 수정의 착수 순서를 강제하지 않는다(§3).**

### P0. 기준선 고정 및 현행 재현 — 부분 확인

**목적:** 관찰 문서의 9/2~9/22 표본과 실제 수정 시작 시점의 코드를 분리한다.

- 시작 시 `root`·`server`·`client` 각각의 HEAD, 원격 대비 상태, 미커밋 파일을 기록한다. 현재 작업트리의 다른 변경은 사용자 자산으로 보존한다. 로컬/원격 테스트 서버의 실제 버전도 확인해 코드와 런이 같은 해시인지 표시한다.
- N-10은 한 턴의 `eventId`·현재 장소·주 NPC·`choiceSurface`·노출 `choiceIds`·`llmChoices`·최종 UI 선택지를 함께 추적한다. 과거 기록에서 서술은 회계사 장면, `choiceSurface=server`와 `mkt_enc_bus_*`는 악사 이벤트, `llmChoices`는 회계사 중심이었다. **두 후보 중 어느 것을 왜 최종 노출했는지**가 핵심이다.
- N-11은 저작 이벤트 `EVT_GUARD_INT_3`가 경비대 지구 외곽 술집을 배경으로 한다는 점을 포함해, 입구→술집의 이동이 이벤트 선택·프롬프트·본문 중 어디에서 생략됐는지 확인한다. 단순히 “다른 장소 이벤트 오매칭”으로 단정하지 않는다.
- N-12, N-14, N-16은 현행 재현 여부를 따로 기록한다. N-14는 서버의 최근 화자 후처리 커밋 이후 스트리밍·완료 본문 양쪽을 본다. N-16은 현재 아크 스테이지/명시적 피날레 선택(arch/103)을 거치는 완주로 확인한다.
- 자동 점수만 보지 않고 **행동 입력 → 서버 판정/상태 → 최종 본문 → 실제 노출 선택지**를 같은 턴에 나란히 놓는 읽기용 비교표를 만든다. 개인 계정·실유저 데이터는 쓰지 않는다.

**산출물:** 재현 여부와 최신 해시를 적은 표; 대표 고정 픽스처(회계사 협박, 경비대 외곽 술집, 이렌 공통몽, 행동 강행, 화자 경계, 피날레). **통과 조건:** N-10~16 각각에 `현행 재현 / 해소됨 / 조건부 / 판단 보류`와 근거가 붙는다. 보류를 결함 확정으로 넘기지 않는다.

### P1. 장면·행동·선택지의 단일 정합 — 부분 구현·일부 실런 확인

**대상:** N-10, N-11, 현행 재현 시 N-12.

1. LOCATION 턴에서 이미 서버가 가진 `locationId`, 활성 `eventId`, `primaryNpcId`, 플레이어 입력/판정, 선택지 출처를 **한 턴 계약**으로 명시한다. 새 게임 상태를 만들어 저장하는 것이 아니라, 기존 값이 서버 기본 선택지·LLM 선택지·서술 프롬프트·최종 UI까지 어떻게 흐르는지 확인하고 어긋나는 지점을 좁힌다.
2. 서버 기본 선택지와 워커의 `finalChoices` 중 무엇이 유저에게 노출되는지 단일 경로로 추적한다. 현재 `EventChoiceGate`는 NPC 일치만 보는 경우가 있으므로 **NPC가 없거나 다른 이벤트의 선택지가 남는 사례**까지 검증한다. 선택지 자체의 `sourceEventId`/장면 출처를 확인하는 방안은 진단 후 최소 변경으로 결정한다. 오해를 막기 위해 선택지 라벨만 기계적으로 다른 이름으로 치환하지 않는다.
3. 이벤트가 현재 구역 안의 **하위 무대**(경비대 외곽 술집 등)로 옮길 때는 본문 첫머리의 장소 연결을 명시한다. 실제 위치 상태가 바뀌지 않는 사건이라면 그 범위도 문장에서 분명해야 한다. 프롬프트의 기존 `sceneFrame` 연속성 규칙을 우선 확인한다.
4. 강한 자유 입력은 서버 판정 결과에 맞춰 **시도 → 상대·세계의 반응 → 결과**가 최소 한 번 드러나도록 한다. 싸움을 반드시 성공시키거나 전투로 강제하지 않는다. 무산·저지·실패도 플레이어 행동에 대한 구체적 결과여야 한다.

**코드 탐색 시작점:** `server/src/turns/location-turn.service.ts`의 이벤트/선택지 조립, `turns.core.ts`·`event-choice-gate.spec.ts`, `engine/hub/event-matcher.service.ts`·`npc-resolver.service.ts`, `llm/llm-worker.service.ts`의 최종 선택지 저장, `content/*/events_v2.json`. **선행 설계:** arch/68, arch/98, arch/104·105.

**통과 조건:** N-10/11 고정 픽스처에서 본문과 실제 노출 선택지의 무대·상대가 맞고, 하위 무대로 옮기는 경우 연결 문장이 보인다. N-12가 현행 재현되면 행동이 단순 언급에 그치지 않는 회귀 테스트가 통과한다. 10~15턴 표적 런에서 심각한 장면·선택지 불일치 0건. 기존 자유 입력·NPC 소개·경비대 이벤트의 의도된 동선은 유지한다.

### P2. 사실 정본·화자 경계 보호 — 정본 부분 수정·일부 실런 확인

**대상:** N-13, 현행 재현 시 N-14. N-08의 중요한 숫자도 여기서 정본 여부를 분류한다.

1. 이렌의 공통몽에서 **사흘간 깨어나지 못했다**는 정본을 `facts.json`·`npcs.json`·프롤로그·관련 이벤트/프롬프트·요약 메모리까지 대조한다. 내용이 다른 저작 지점이 있으면 먼저 정본을 통일하고, 생성 결과의 역전(못 잠 ↔ 못 깨어남)은 고정 재현으로 막는다. 의도적인 거짓말이라면 명시적 화자 동기·후속 폭로가 있는 경우만 예외로 둔다.
2. 숫자·시점·가격처럼 **정확히 기억해야 하는 단서**와 분위기용 세부를 구분한다. 핵심 사실은 식별 가능한 fact/상태를 출처로 삼고, 실제 단서가 아닌 임의의 정확한 숫자를 새 퀘스트 정보처럼 부풀리지 않는다. 숫자 자체를 전면 금지하지 않는다.
3. N-14가 현행 재현되면 최근 JEV 기반 화자 후처리와 기존 `narrative-postprocess`의 스트리밍 경계·익명 화자·완료 본문 경로를 함께 본다. 메인 서술의 대사 형식과 UI 렌더링은 별개로 검증한다. 비대사 인용이나 간접화법을 NPC 대사로 오귀속시키지 않는다.

**코드·콘텐츠 탐색 시작점:** `content/star_sand_v1/facts.json`, `npcs.json`, `scenario.json`, `server/src/llm/prompts/prompt-builder.service.ts`, `llm/worker/narrative-postprocess.service.ts`, `llm/narrative-filter.core.ts`. **통과 조건:** 정본-본문 역전 0건(표적 픽스처와 두 극야해안 단기 런); 화자 문제는 현행 재현 시 스트리밍·완료 렌더 양쪽에서 대사 누락·오귀속 0건. 단서 사실과 일반 비유를 혼동하는 검사는 오탐을 먼저 검토한다.

### P3. 대화 → 새 사실/관계 → 다음 행동의 연결 — 부분 구현·오유도 재검증 중

**대상:** N-05, N-06, N-07, N-08.

1. 같은 NPC와 2~4턴 이어 말한 표본을 `질문`, `이미 공개된 fact`, `이번 턴의 판정`, `실제 새 fact/관계/현장 변화`, `NPC가 알 수 있는 것`으로 읽는다. 현재 구현된 `EmptyHandedHint`·`QuestForwardChoice`·`recentTopics`·대화 잠금이 왜 범용 선택지 또는 재암시로 귀착했는지 **원인별로 나눈 뒤** 바꾼다.
2. 대화 턴은 새 사실을 항상 강제하지 않는다. 합당한 반응의 경로를 `직접 답변`, `부분 답변과 확인 가능한 단서`, `관계/태도 변화`, `이유가 있는 거절과 특정 다음 경로`로 나눠 본문에서 하나 이상 읽히게 한다. 모르는 NPC가 증거를 지어내지 않게 보유 지식·공개 조건을 유지한다.
3. 사실을 얻거나 명확한 인계를 받은 다음에는 그 턴의 **실제 대상/사물/장소**를 참조하는 선택지를 최소 하나 고려한다. 선택지가 장면 고유 행동을 표현해도 `sourceNpcId`, affordance, 위험도, 클릭 후 판정이 일치해야 한다. 자유 입력은 대체하지 않는다.
4. 에드릭처럼 숫자를 쓰는 인물은 숫자가 `정본 단서`, `개인의 추정`, `단순 습관적 말버릇` 중 무엇인지 독자가 구분할 단서를 준다. 정본이 아닌 정밀 수치로 추리 퍼즐을 꾸미지 않는다.

**코드 탐색 시작점:** `server/src/engine/hub/quest-progression.service.ts`, `server/src/turns/empty-handed-hint.core.ts`·`quest-forward-choice.core.ts`, `llm/prompts/prompt-builder.service.ts`, `llm/nano-event-director.service.ts`, `llm/worker/worker-narrative.core.ts`. **선행 설계:** arch/45·98·104와 9/2 빈손 대화 가드.

**통과 조건:** 이렌·에드릭을 각각 연속 대화하는 표적 런을 정독했을 때 매 턴 답변/변화/정당한 거절 중 무엇이 일어났는지 구분할 수 있고, 같은 화제의 재암시가 새 사실처럼 반복되지 않는다. 단서 획득 후 행동 가능한 경우 다음 선택지에 장면 고유 후속 행동이 있다. 범용 선택지 빈도는 P0 기준선과 **동일 분모·동일 선택지 표면**으로 비교한다. 무의미한 선택지 치환이나 NPC 정보 누출로 수치를 개선하면 불합격이다.

### P4. 시작 장면의 동기와 이동 턴의 리듬 — 부분 구현·일부 실런 확인

**대상:** N-02, N-03, N-04. N-01은 보존 게이트.

1. 그레이마르는 로넨과 장부가 **누구에게 어떤 위험**을 주는지 초반 본문에서 하나의 구체적 결과로 읽히게 한다. 정본 밖의 새 범죄·사형 확정·거액 보수를 추가하지 않는다. 신비가 필요한 정보와 첫 행동의 동기를 구별한다.
2. 극야해안의 여관 재진입은 막지 않는다. 선택 시 “방금 나왔던 장소에 다시 들어가 확인할 이유”가 장면이나 선택지에서 읽히게 한다. 이미 알려진 상태를 반복 낭독하기보다 여관 안에서만 확인 가능한 사물·인물에 집중한다. `hub` 추상 공간과 실제 여관의 이름/위치 구분(arch/92)을 회귀시키지 않는다.
3. 단순 이동은 시간·지형을 짧게 연결하고, **실제 있는 경우에만** 변화한 사람·상태·위험을 전면에 둔다. 사건이 없는 모든 이동에 새 사건을 강제로 붙이지 않는다. 이동 비용·시간대와 플레이어의 장소 선택권은 기존 서버 규칙대로 둔다.

**통과 조건:** 두 팩 신규 진입 10~15턴 정독에서 의뢰 이유를 한 문장으로 재설명할 수 있고, 동일 장소 재방문이 의도적 행동으로 보인다. 이동·진입 본문이 직전 위치/시간대와 모순되지 않으며, 짧아져도 다음 장면의 공간 이해를 해치지 않는다. 첫 장면의 공포·항구 분위기(N-01)는 유지된다.

### P5. 결말의 인과와 체감 보상 — 확정 맥락 전달 부분 구현·현행 완주 대기

**대상:** N-16(현행 재현에 따라 범위 결정).

- 결말의 실제 체감 품질·완료를 판정하기 전에 현재 코드로 명시적 `arc_finale`까지 가는 **완주 표본**을 확보한다. 9/2 기록의 150↔20골드·결말 추상성은 회귀 후보이지 현행 결함 확정이 아니다. 그 전에도 서버 확정값을 결말 프롬프트에 연결하는 독립 계약은 단위 테스트로 개선할 수 있다.
- 현재 `ArcState`·stageProgress·ending reason·선택된 루트·확정된 비용/보상과 결말 본문을 대조한다. arch/103의 **커밋 후 3막 → “결말을 맞이한다” 명시 선택지**를 다시 설계하지 않는다.
- 결말 본문은 `선택한 루트`, `핵심 증거의 귀착`, `직접 관계된 NPC 또는 세력의 즉각적 변화`, `지불한 대가`, `남는 불확실성`을 우선순위 순으로 드러내는 계약을 세운다. 모든 갈래를 같은 여운 문단으로 평탄화하지 않는다. 서버가 확정하지 않은 처벌·금액·아이템은 단정하지 않는다.

**통과 조건:** 두 팩에서 가능한 루트/안전망을 대표하는 완료 경로로 테스트한다. 구체 비용·선택·엔딩 루트와 본문 충돌 0건, 종결권이 플레이어에게 있다는 UI 계약 유지, 결말 뒤 독자가 “내 선택으로 무엇이 달라졌나”를 두 문장으로 말할 수 있다. 긴 테스트는 엔딩 검증 목적을 명시하고 수행한다.

### P6. 문장 리듬·표면 마감 — 지시 부분 수정·일부 실런 확인

**대상:** N-09, N-15. N-14의 현행 재현 여부도 최종 확인한다.

- 구조 수정 이후 동일한 장소·NPC 연속 턴을 다시 읽고, 새 정보 없이 붙은 감각 묘사만 줄인다. 환경 묘사를 제거하는 것이 아니라 **행동·반응·발견 안에 감각을 묶는 방향**이다.
- 이미 있는 arch/110의 개시 변주와 `scripts/analyze_narrative_structure.py`를 재사용한다. 금지어 목록·완성 문장 예시를 프롬프트에 늘리지 않는다(구체 어휘 주입의 복제 위험, 불변식 50). `냄새/시선/손끝` 출현 횟수는 경향 확인용이고 단독 통과 지표가 아니다.
- 영어 잔해, 인용부호 연결, 화자 불명확 문장을 `scripts/audit_quality.py`의 정본 감사와 실제 본문 정독으로 확인한다. 후처리로 의미를 바꾸거나 비대사를 화자 마커로 포장하지 않는다.

**통과 조건:** 같은 NPC의 연속 대화에서 매 턴 공간이 처음부터 리셋되는 느낌이 줄고, 필수 단서·인물 어조는 보존된다. 언어·따옴표 오류 0건(표적 픽스처), 자동 품질 감사와 사람이 읽는 문장 모두 통과. 분량·반복 지표가 좋아져도 사건 정보가 누락되면 불합격이다.

### P7. 통합 검증과 출시 판단 — 두 팩 단기 실런 부분 진행·출시 판단 대기

- P1~P6을 합친 두 팩의 신규 진입, 3턴 이상 동일 NPC 대화, 다른 지역 이동, 강행 자유 입력, 단서 획득, 종반 경로를 통합 정독한다. 가능한 한 `chatty`·`coercer`·관찰 성향을 섞고 **표준 플레이테스트는 10~15턴 여러 런**으로 누적한다. 완주만 별도 긴 런을 쓴다.
- 서버/클라 변경 레포에서 각각 필요한 테스트와 `pnpm build`를 돌린다. 정본 `scripts/playtest.py`의 V0~V14, `scripts/audit_quality.py <run_id>`, 콘텐츠 정합 감사, 기존 스냅샷의 **diff 내용**을 함께 검토한다. 자동 PASS는 독자 경험 PASS로 간주하지 않는다.
- 변경 전후를 같은 시나리오·행동 유형·모델 설정·선택지 표면으로 비교한다. 속도/비용/프롬프트 예산도 함께 본다. 핵심 경로에 LLM 호출을 늘렸다면 지연 p50/p95와 실패율의 악화가 정성 이득보다 크지 않은지 별도 승인 관문을 둔다.
- 이 계획과 관찰 문서의 N-01~16에 `해결/완화/현행 미재현/미해결`을 실제 근거와 함께 갱신한다. 개선이 증명되지 않은 항목을 완료 처리하지 않는다.

**최종 출시 조건:** P0에서 확정된 심각한 장면·사실 정합 결함의 재발 0건, 두 팩의 연속 대화와 이동 경로에서 네 독자 질문에 답할 수 있음, 명시적 결말의 상태-서술 일치, 기존 테스트·빌드·품질 게이트 통과. 배포·푸시는 해당 시점의 별도 작업 범위와 서버 상태를 확인한 후 진행한다.

## 3. 회차 운영 규칙

한 단계의 완료 판정에는 **현재 재현 → 원인 추적 → 최소 범위 구현 → 표적 테스트 → 관련 실런 정독 → 필요 시 재수정**의 증거가 필요하다. 다만 2026-09-25 사용자의 진행 우선 결정에 따라, 한 단계의 비중대 결함이 다음 단계의 조사·독립 수정을 막지는 않는다. 로컬 커밋도 회차마다 자동으로 하지 않고 명시적 커밋 요청이 있을 때 변경 범위를 검토한다. 서로 다른 레포의 변경이 한 기능을 이루면 버전·배포 순서를 명시해 중간 상태에서 깨지지 않게 한다.

**진행 중단 기준:** 서버 판정과 본문이 정면으로 반대이거나, 장소·대상·선택지가 다른 사건을 가리키거나, 핵심 정본 사실·실제 비용/보상이 뒤집히거나, 선택지가 클릭 불가능한 경우. 이런 결함은 재현·표적 수정 전 출시 판단을 보류한다. 반면 드문 어색한 동작, 금지 표현 1~2건, 단일 런의 과다 화자·반복 어휘는 발생 빈도와 체감 영향을 기록하고 다음 독립 단계를 진행할 수 있다. **단계 진행과 단계 완료/출시 승인은 다른 판정**이다. 추가 모델 호출·의미 검출기·후처리 강제 삽입은 기본 해법으로 쓰지 않고, 재현 빈도와 지연 영향을 본 뒤 결정한다.

| 단계 | 상태 | 다음 단계 진입 게이트 |
|---|---|---|
| P0 기준선 | 부분 확인 | 현재 코드 해시·재현 분류·고정 픽스처 확보 |
| P1 장면/행동/선택지 | 첫 턴 지목 공격의 사건 탈취 수정; 성공 공격의 밀치기·주먹 접촉 실런 확인, 다른 판정·레닉 첫 조우 대기 | 중대한 사건/대상 불일치 0, 남은 성공 서술은 표적 추적 |
| P2 사실/화자 | N-13 정본 확인, N-14 클라이언트 완료/SSE 단위 검증, 실런 `닢` 1건·정밀 수치·실제 브라우저 확인 미해결 | 핵심 정본 충돌 0, 스트림·완료 화면 회귀 확인 |
| P3 대화/선택 진전 | N-17 오유도 수정, 최근 3턴 STEER 재삽입 방지 및 생성 질문 유사 반복 제거·단위 검증 | 같은 NPC 연속 대화의 진전·반복 선택지 수정판 실런 확인 |
| P4 도입/이동 | 두 팩 신규 진입·이동 경로 단기 실런 확인, 그레이마르 수락 장면의 로넨 위험 환기 보강·첫 턴 확인 | 첫 행동의 이유·재방문 목적·이동 연결이 읽힘 |
| P5 결말 | 그레이마르 고발 노선 명시 완주 2회; 자동 조기 종료·미지급 루트 보상·과장된 에필로그 수정, 다른 루트·팩과 본문 인과는 대기 | 현행 완주 표본의 선택·비용·파급 효과 정합 |
| P6 문장 마감 | N-18 방어, 극야해안 금지 표현 2건·반복 어휘 후속 관찰 | 핵심 정보 보존 + 고빈도 표면 오류·정형감 재검토 |
| P7 통합/출시 | 두 팩 단기 런·그레이마르 고발 결말 확보; 실런 금지 화폐·대사 마커·반복 어휘와 인증 화면·다른 결말 미해결, 출시 판단 대기 | 두 팩 정독, 테스트·빌드·감사, 전 항목 상태 갱신 |

각 단계의 결과는 이 표의 상태뿐 아니라 **기준 커밋, 변경 커밋, 재현 파일·런 ID, 실패/통과 근거, 남은 위험**을 문서에 남긴다. 실행 중 다른 결함이 발견되면 기존 단계에 억지로 끼우지 말고 N-번호를 새로 부여해 심각도와 의존 관계를 재평가한다.

## 4. 2026-09-25 부분 구현 기록

- 시작 기준: root `42588c9`(원격보다 1커밋 앞, 기존 문서·아티팩트 미커밋 변경 보존), server `3690f92`(5커밋 앞), client `9254daa`(원격과 동일; 변경 없음). `/v1/version`으로 읽은 가동 서버는 `5d94316`, 시작 시각 `2026-09-22T05:54:40Z`였다. **그 시점의 가동 서버는 작업 시작 server HEAD보다 오래되어 수정의 실런 검증 대상으로 사용할 수 없었다.** 이 기록 작성 당시 재시작·푸시·배포는 하지 않았다.
- P0/N-10: `qc_20260922_cycle1_graymar_coercer.json` T4의 `eventId=SIT_ACTIVITY_NPC_EDRIC_VEIL_1`, `primaryNpcId=NPC_EDRIC_VEIL`인데 같은 응답의 서버 `choiceIds=mkt_enc_bus_*`였다. `choiceSurface=server`는 *그 턴 입력을 고른 직전 화면의 출처*이며 이번 응답의 최종 선택지 출처가 아니다. 생성 완료 후 `llmChoices`는 에드릭 맥락이었다. 원인은 `SituationGeneratorService`가 악사 이벤트를 동적 활동의 템플릿으로 삼으면서 `payload.choices`까지 복사한 것. NPC 활동·갈등·결과·환경 파생 상황에서 템플릿 고유 선택지를 비웠다. 빌드한 서비스의 같은 조건(`EVT_MARKET_ENC_BUSKER` 템플릿, 에드릭 현재 NPC) 직접 호출에서 `SIT_ACTIVITY_NPC_EDRIC_VEIL_1`, 에드릭 장면, 선택지 `[]`를 확인했다. 실제 서버·클라이언트의 초기/완료 선택지 재검증은 남았다. 이 변경은 선택지 출처만 격리한다. 템플릿의 `tags`·affordance·보상 등 다른 필드의 동적 장면 상속은 별도 검토가 필요한 잔여 위험이다.
- P1/N-11: `EVT_GUARD_INT_3`가 `LOC_GUARD` 소속의 외곽 술집 장면인 것은 의도된 정본이다. `sceneFrame`에 입구에서 골목을 따라 열린 술집 문 앞까지의 동선을 추가했다. 문구가 실제 완성 본문에 남는지는 실런에서 확인해야 한다. N-12 강행 행동은 과거 표본만 있어 수정하지 않았다.
- P2/N-13: `FACT_SS_FIRST_DREAM`의 핵심을 *잠을 못 잔 사흘*이 아니라 *잠든 뒤 깨어나지 못한 사흘*로 명시했다. `scenario.json` 기억의 “추락 첫날 밤”은 사실의 “추락 전날 첫 공통몽”과 어긋나므로 같은 시점으로 바로잡았다. N-14는 최신 화자 후처리 경로를 현재 서버에서 재현하지 못해 보류했다.
- P3/N-05·06: 다음 미발견 사실의 보유자/발견 장소가 **현재 장소 밖**으로 확인된 경우, 신규 fact 획득 턴에 기존 `QuestForwardChoice`를 바로 내도록 했다. 현 장소에서도 발견 가능하거나 보유자가 같은 장소면 기존 차단을 유지한다. 범용 선택지 전체나 반복 회피 문구는 아직 손대지 않았다.
- P4/N-02~04: 로넨이 도난 장부 담당자이고 이미 누군가에게 쫓긴다는 기존 정본을 프롤로그에 드러냈다. 극야해안 여관 `hubHint`는 장부·실종 손님 방이라는 재방문 목적을 제시한다. HUB 이동 지시는 1문단 2~3문장·목적지 입구 종료로 좁혔고, 이동 턴의 주인공 과거 재소개 지시를 제외했다.
- P6/N-09: 같은 장소 5턴 이상일 때 시각·청각·촉각과 새 소품을 매번 강제하던 지시를, 직전 행동/반응 중심의 장면 연속성 지시로 바꿨다. N-15 실제 영어 잔해는 후처리 규칙을 추가하지 않고 재검증 대기다.
- 검증: server `pnpm build` 통과, Jest **200 스위트·2712 테스트 통과**(1 스위트·2 테스트 기존 skip), 프롬프트 골든 스냅샷 1건 의도된 차이만 갱신, `scripts/audit_content.py` 전 팩 ERROR/WARN 0(기존 INFO 36). 현재 런타임과 작업 코드의 버전 불일치로 정본 `scripts/playtest.py`·`audit_quality.py`는 아직 실행하지 않았다. 따라서 P1~P6의 체감 품질이나 완료 조건을 통과했다고 간주하지 않는다.
- 다음 관문: 서버 코드를 안전하게 반영할 시점을 정한 뒤 두 팩 10~15턴 표적 런을 수행한다. N-10 서버 즉시/최종 UI 목록, N-11 동선, N-13 반전, 신규 단서 후 선택지, 장기 대화 문장 리듬을 실제 본문과 함께 정독한다. N-12/14/16은 현행 재현부터 판단한다. 이 기록 작성 당시 변경은 미커밋이며 푸시·서버 재시작도 없었다.

## 5. 2026-09-25 배포 검토와 후속 피드백

- 부분 구현은 root `89eb7cd`, server `09215dd`로 푸시되고 서버에 반영됐다. 가동 서버 `/v1/version`은 `09215dd`를 반환했다. 두 팩 각 10턴 실런 ID는 그레이마르 `126e9855-c529-4772-a739-1b00a007a9f6`, 극야해안 `40ca0c8e-45d0-489c-8a2e-06f766cafaf3`이다. 원본 JSON은 각각 로컬 `playtest-reports/narrative_quality_20260925_graymar_coercer.json`, `playtest-reports/narrative_quality_20260925_star_sand_chatty.json`에 보존되며 이 디렉터리는 Git에서 제외된다. 정본 `audit_quality.py`는 양쪽 모두 자동 위반 0건이었다. 그레이마르 V2 조우 다양성만 1종 NPC 표본이라 미달(14/15), 극야해안은 15/15였다. 이는 서사 관문 전체 통과를 뜻하지 않는다.
- N-10은 그레이마르 4턴 `SIT_ACTIVITY_NPC_EDRIC_VEIL_1`의 서버 `choiceIds=encounter_talk|encounter_observe|encounter_help|go_hub`로 악사 선택지 유출이 사라졌다. N-13은 극야해안 7턴 이렌이 “잠이 들었고, 사흘 동안 한 번도 눈을 뜨지 못했어요”라고 말해 정본과 맞았다. N-11 경비대 술집 이벤트는 이번 런에서 재현되지 않았다. 따라서 P1·P2 완료로 올리지 않는다.
- 신규 높은 우선순위 피드백: 같은 극야해안 7턴의 첫 공통몽 사실은 후속 방향으로 여관 주인·장례사를 가리키는데, 즉시 선택지 `quest_forward_7`이 등불수녀원으로 보냈다. 원인은 발견 직후 정체 카운터를 강제로 채운 뒤, **이번 발견 사실이 아닌** 다음 미발견 사실의 위치로 전진 선택지를 만든 것. 후속 수정은 새 사실의 정본 `nextHintLocationIds`에 실제 이동 목적지 ID가 있을 때만 즉시 노출하고, 빈손 인계·3턴 정체 경로는 유지한다. 장소명이 산문에 단순 등장하는지만 검사하던 첫 수정안은 부정문·복수 장소를 오판할 수 있어 코드 검토에서 폐기했다. 구조화된 ID 판정은 이 기록 작성 시점에 실서버에 아직 반영되지 않았다.
- N-11 문구는 반복 가능한 `EVT_GUARD_INT_3`에서 매번 “경비대 지구의 입구에서” 출발한다고 단정하던 문제가 있어, 현재 위치와 무관한 외곽 술집 무대 묘사로 조정했다. 실제 생성 본문에서 공간 연결이 읽히는지는 표적 런에서 확인해야 한다.
- 별도 P2/P3 과제: 그레이마르 회계 대화의 정본에 없는 정밀 수치·`닢`, 극야해안의 `젓가락`이 본문/선택지에 생성됐으나 `audit_quality.py` 위반 0건이었다. 감사 통과만으로 정본 사실·세계관 표면을 승인하지 않는다. 우선 즉시 이동의 오유도와 고정 출발 문구를 재검증한 뒤, 수치의 출처와 감사 사각지대를 P2/N-08 경로에서 다룬다.
- **다음 관문:** 수정 코드가 실서버에 반영된 버전으로 이렌의 첫 공통몽 직후 선택지, 경비대 이벤트의 반복 위치, 빈손 인계/정체 후 전진을 표적 확인한다. 이 관문이 통과하기 전에는 P1→P2 완료 판정이나 다음 대규모 수정을 진행하지 않는다.

## 6. 2026-09-25 후속 실런과 잔여 관문

- 후속 변경은 root `205d4d8`(경비대 술집 재진입 문구), `da6d15c`(새 사실의 명시적 후속 장소 ID), server `f4d6d07`·`6206a02`(즉시 전진 판정)를 로컬 커밋으로 남겼다. server `6206a02`를 재시작해 `/v1/version`으로 확인했고, 빌드·전체 Jest·콘텐츠 감사와 스모크를 통과했다. 이 절의 실런은 **그 가동 버전**에서 수행했다.
- N-17: 극야해안 10턴 런 `eecce853-35f8-4159-b37b-5acf229d6605`의 T7에서 이렌의 `FACT_SS_FIRST_DREAM`을 얻었고, 노출 선택지는 `encounter_talk|encounter_observe|encounter_help|go_hub`였다. 잘못된 `quest_forward_7`은 나오지 않았다. 같은 사실의 후속 목적지는 `nextHintLocationIds`로 저작해 산문 내 장소명 언급·부정문에 기대지 않도록 했다. 별도로 빈손 인계와 3턴 정체 후 전진은 단위 테스트로 보존했다. 새 필드의 배열 모양·중복·팩 내 참조·표시 문구는 콘텐츠 감사에서 검사한다.
- N-11: 그레이마르 10턴 런 `59c42140-d01f-45ed-8c8a-5cef59ab921b`의 T12에서 `EVT_GUARD_INT_3`가 실제 발화했다. 플레이어의 지목/최종 화자는 경비병 펠릭스였지만 이벤트 원본 NPC는 레닉이었다. 기존 게이트가 레닉 선택지는 버렸으나 술집 `sceneFrame`은 남겨, **문구 수정만으로는 해결되지 않았다.** 검토 과정에서 무대만 숨기면 레닉 이벤트의 효과·쿨다운·이력이 남고, 이벤트 선택 후 교체하더라도 자율 비트의 채택 상태가 이미 바뀔 수 있음을 확인했다. 후속 서버 수정은 ACTION의 확실한 현장 NPC 지목을 **이벤트 라우팅 전에** 계산해 플레이어 우선 분기로 전달한다. 첫 진입 등으로 저작 이벤트가 여전히 선택되고 저작 NPC와 지목 NPC가 다르면 판정 전에 이벤트 전체를 `FREE_PLAYER` 셸로 교체한다. 이 사후 교체는 상태를 선변경할 수 있는 자율 비트/동적 상황에는 적용하지 않는다. 레닉 전용 배경·선택지·효과·이력은 적용되지 않아야 한다. 늦은 Resolver만 지목을 잡는 예외 경로는 기존 선택지 게이트가 남으므로 별도 관찰이 필요하다. 새 경로의 실서버 재현은 아직 관문이다.
- N-18: 같은 극야해안 런 T10 이동 본문에 `turn_summary`·`location`·`conditions` 키가 담긴 배열형 계획 문구가 노출돼 `audit_quality.py`가 실제 영어 위반 5건을 검출했다. 기존 서술 형태 방어는 `{`로 시작하는 객체만 검사했다. 후속 서버 수정은 JSON 배열과 파싱 실패한 배열형 키-값 계획을 거부해 기존 재시도·대체 모델 경로로 보낸다. 또한 첫 유효 문자가 `{`/`[`인 스트림은 형태 검증 전 SSE 출력을 보류해 잠깐 보였다가 사라지는 노출을 막는다. 보통 산문은 곧바로 스트리밍하며, `[장면]` 같은 정상 괄호 시작 산문은 최종본에서 보존되지만 그 턴의 점진 표시만 지연된다. 표적 단위 테스트는 있으나 수정 버전의 실런·품질 감사는 남았다.
- 두 런 모두 정본 플레이테스트 V0~V14는 15/15였지만, 이는 사람이 확인한 N-11/N-18 결함을 상쇄하지 않는다. 자동 감사는 그레이마르 0건, 극야해안 5건이었다. 원본은 로컬 Git 제외 경로 `playtest-reports/narrative_quality_20260925_final_graymar_coercer.json`, `playtest-reports/narrative_quality_20260925_final_star_sand_chatty.json`에 있다.
- **판정:** N-17의 표적 실런은 통과. N-11·N-18 후속 코드를 서버에 반영해 재시험하기 전에는 P1·P6을 완료 처리하지 않고, 다음 큰 단계로 넘어가지 않는다.

## 7. 2026-09-25 수정 서버 `a17c4f3` 재검증

- root `0bcfe72`(콘텐츠 감사·계획 기록), server `a17c4f3`(이벤트 대상 선판별·배열형 서술/스트림 방어)를 로컬 커밋했다. 서버를 재시작했고 `/v1/version`은 `a17c4f3`, 시작 시각 `2026-09-25T12:13:18.833Z`였다. 3턴 스모크 PASS, 서버 빌드 PASS, 전체 Jest 201 스위트·2726 테스트 PASS(기존 1스위트·2테스트 skip), 콘텐츠 감사 ERROR/WARN 0. **이 커밋들은 푸시하지 않았다.**
- 그레이마르 10턴 실런 `14d22c64-585e-4e43-be95-b601044036e9`: V0~V14 **15/15 PASS**, 정본 품질 감사 실제/회색 위반 각 0건. T3 `SIT_ACTIVITY_NPC_EDRIC_VEIL_1`의 서버 선택지와 워커 최종 `llmChoices` 모두 에드릭 맥락이고 악사 선택지 유출이 없었다(N-10). T10(게임 turnNo 13)은 실제로 “경비병에게 다가가…”를 입력해 `FREE_PLAYER_13`, `primaryNpcId=NPC_GUARD_FELIX`로 판정됐다. 본문/선택지에 레닉 술집 배경·선택지가 없었다(N-11 지목 경로). 레닉을 **나중에 실제 지목해 첫 조우 선택지가 유지되는지**는 이번 10턴에 표본이 없어 미확인이다.
- 극야해안 10턴 실런 `4d066f7f-eb30-4909-92fc-945adc8dba4e`: V0~V14 **15/15 PASS**, 정본 품질 감사 실제/회색 위반 각 0건. T6(게임 turnNo 7) 이렌의 첫 공통몽 대화 직후 서버 선택지 `encounter_talk|encounter_observe|encounter_help|go_hub`로 등불수녀원 오유도 `quest_forward_7`은 없었다(N-17). T7 여관 이탈/T8 부두 이동의 최종 본문에는 JSON형 계획 문구가 없었다. N-18의 비정상 모델 출력 자체는 재발하지 않아, **스트림 중 노출 차단은 분할 청크 단위 테스트와 워커 배선 테스트 근거**이며 실모델 재현 통과로 과장하지 않는다.
- 두 원본은 Git 제외 경로 `playtest-reports/narrative_quality_20260925_postfix_graymar_coercer.json`, `playtest-reports/narrative_quality_20260925_postfix_star_sand_chatty.json`에 보존했다. 정본 테스터의 포인트가 0이라 새 테스트 전용 계정(가입 기본 50p)을 사용했고, 충전·결제는 하지 않았다. 기본 `DESERTER`로 극야해안 런 생성에 실패한 1회는 유효 프리셋 `SS_DOCKHAND`로 재시도했으며 턴/포인트 소비 전이었다.
- **다음 관문:** N-11 레닉의 정당한 첫 조우 및 후단 Resolver에서만 NPC가 식별되는 예외를 표적 재현하고, N-18은 배열형 응답 재발 시 최종 본문·SSE 모두에서 노출 0인지 확인한다. N-12/14/16은 아직 현행 재현/완주가 부족하다. 따라서 P1·P2·P6 완료 판정과 P7 출시는 보류한다. 다음 큰 수정은 P2/N-08의 정본 없는 정밀 수치·세계관 어휘와 P3 연속 대화 진전으로 진행하되, 위 관문을 동시에 추적한다.

## 8. 2026-09-25 잔여 관문 진행 — 로컬 변경, 실서버 미반영

- 기준은 root `367e02c`, server `a17c4f3`, client `9254daa`. 기존 JEV 문서·실험 스크립트와 클라이언트 주사위 파일의 미추적 변경은 이 작업과 무관하므로 보존한다. 이번 절의 수정은 아직 **커밋·푸시·서버 재시작 전**이다. 따라서 아래 표적 테스트는 새 코드의 단위 검증이며 새 코드의 실런 통과로 해석하지 않는다.
- P1/N-12 현행 재현: 수정 전 서버에서 `scripts/playtest.py --turns 10 --scenario graymar_v1 --preset DESERTER --agent brawler` 런 `5ca3e4af-a7bc-444e-9d07-d25103a42062` (`playtest-reports/narrative_quality_20260925_brawler_baseline.json`)을 얻었다. 플레이어 공격 입력 세 턴(게임 T5~7)의 `parsedType=FIGHT`, `resolveOutcome=FAIL`인데, 본문은 첫 턴에 거짓 외침과 노부인의 훈계로 대체했고 후속 턴에도 주먹의 시도·차단·실패가 명료하지 않았다. 판정 매트릭스의 `FAIL.FIGHT='반격당함.'`은 서버가 확정하지 않은 반격을 지시하면서 원래 행동 보존을 지시하지 않았다. `PARTIAL.FIGHT`의 확정 상처도 같은 정본 위험이었다. 이제 해당 턴에 공격 시도·부분 성과/실패를 드러내되 반격·피해는 서버 사건/상태에 있을 때만 확정하도록 지시한다. 새 서버에서 같은 행동을 되풀이한 본문 검증은 남았다.
- P2/N-08: 이전 그레이마르 런 `126e9855-c529-4772-a739-1b00a007a9f6` T7의 “스물여섯 닢”, “보름째, 열 항목 가운데 일곱”, T5의 “0.7리터”는 서버 사실의 정확 수치와 대조되지 않는 생성 문장이다. 극야해안 런 `40ca0c8e-45d0-489c-8a2e-06f766cafaf3` T7은 “젓가락을”을 생성했다. 프롬프트에는 TALK 성공 시 정본에 없는 “3번 창고” 완성 예시까지 주입되고 있었다. 완성 예시를 제거하고, 증거처럼 읽히는 정확한 금액·날짜·횟수·장소 번호는 서버 사건·공개 정보·확립 사실이 있을 때만 단정하도록 P0에 추가했다. `젓가락`도 세계관 금지어로 명시했다. 정본 감사기는 한글 수사 뒤의 `닢`과 조사 붙은 `젓가락`을 놓치던 패턴을 보완했다. 변경 후 기존 두 런 재감사에서 각각 실제 위반 1건을 검출했다. **수치 사실성 자동 판정은 여전히 없고, 실제 생성 개선은 재실런 필요.**
- P2/N-14 현행 재현: 위 brawler 런 게임 T12에서 “말이 겹쳐 나온다. @[무명 인물] …”처럼 마커가 지문과 같은 줄에 있었고, T14에는 발화자가 식별되지 않은 인용이 남았다. 완료 본문의 인라인 마커는 별도 문단으로 정리한다. 스트림 분류기는 인라인 마커를 지문 세그먼트에서 제거하고 명시 NPC ID/별칭이면 해당 화자로, 무명 마커면 주 NPC 강제 폴백 없이 미식별 화자로 보존한다. 후자는 임의 오귀속보다 안전한 선택이다. 정본 감사의 마커 적용률도 동일한 따옴표 표본을 분모·분자에 써 100% 초과가 나오지 않게 했다. 기존 그레이마르 런 재감사 수치는 23/22(104.5%)→23/23(100%)다. **실제 SSE와 최종 UI 렌더의 동시 확인, T14의 무표기 발화 원인 분류는 남았다.**
- P3/N-05~08 진전 관찰: 같은 이전 그레이마르 런 에드릭 연속 T4~7에서 T4는 본문에 근거 없는 칸 수를 얹고, T5는 실패 판정에도 “23”, “0.7리터”를 새 정보처럼 말하며, T6에는 정본 `셋째 칸 넷째 줄`이 나왔지만 최종 선택지는 장부 설명을 되묻기·강탈하기 등으로 되돌아갔다. T7 성공에도 사실 아닌 정밀 수치가 붙고 다음 선택지 일부는 다시 장부 조사/거래/태도 관찰이었다. 단순한 말투 문제가 아니라 **서술 사실성·선택지 근거·새 fact 이후 후속 행동**의 결합 문제다. 선택지 생성은 완성 본문을 최대 500자 잘라 받아 그 문구를 참조하므로, 본문 환각을 선택지가 재인용할 수 있다. 새 사실/실제 다음 장소를 구조화해 선택지에 전달하는 방향을 검토하되, 정본 없는 숫자를 후처리로 일괄 삭제하거나 모든 대화에 fact를 강제하지 않는다. 이 단계는 아직 미해결이다.
- 검증: 서버 빌드 PASS, 표적 Jest와 프롬프트 스냅샷 17개 갱신(전역 F/K 문구 및 완성 예시 제거 확인), 전체 Jest 201 스위트·2731 테스트 PASS(스트림 분류기 추가 변경 전 실행), `scripts/test_audit_quality.py` 6건 PASS. 스트림 분류기 수정 뒤 표적 39건 PASS 및 서버 재빌드 PASS. **전체 Jest는 최종 변경 후 한 번 더 필요하다.** P1·P2는 예방/부분 수정 단계로 유지하고, P3~P7은 마스터 표의 미해결 상태를 유지한다.

## 9. 2026-09-25 표적 실런 재검증 — 단계 관문 미통과

- 이 절부터는 변경한 서버를 `pnpm build` 후 로컬 launchd 서비스로 재시작해 3턴 스모크를 통과시켰다. `/v1/version`의 `a17c4f3`은 **HEAD 해시만 표시**하므로 미커밋 코드를 포함한 현재 빌드의 내용 해시로 간주하면 안 된다. root/server/client의 작업 시작 HEAD는 각각 `367e02c`/`a17c4f3`/`9254daa`; 이번 변경은 커밋·푸시하지 않았다.
- P1/N-12: 바꾼 프롬프트에서 노부인 대상 강행 런 `fb378817-bd37-4cd7-8e45-127c628e3ffc`(`playtest-reports/narrative_quality_20260925_forced_fight_herbalist.json`)은 FIGHT 실패 T4·5·7에서 시도와 빗나감/회피를 드러냈다. 그러나 부분 성공 T6은 밀친 결과만 보이고 이어진 주먹이 어떻게 되었는지 불명확하다. T4는 명시 지목 대상이 `NPC_MIRELA`인데 첫 장소 턴의 세계 이벤트 우선 규칙 탓에 회계사 동적 사건 `SIT_ACTIVITY_NPC_EDRIC_VEIL_1`이 채택됐다. **첫 장소 턴이라도 지목한 FIGHT는 PLAYER_DIRECTED로 보내는 최소 수정**을 넣고 새 10턴 런 `35df3495-8ddb-4372-b916-ada2eabbc8f9`(`...first_fight_target_gate.json`)으로 T4 `FREE_PLAYER_4`/`NPC_MIRELA`를 확인했다. 판정 SUCCESS에도 완성 본문은 주먹의 접촉·제압을 확정적으로 읽기 어렵고 회계사 대사가 끼었다. 따라서 N-12 행동 정합은 완화됐지만 **통과 아님**. 첫 장소의 일반 TALK 이벤트 우선은 기존 테스트대로 유지한다. N-11 레닉의 정당한 첫 조우도 여전히 미검증이다.
- P2/N-08: 전역 정본 수치 경고와 사실 공개 블록의 수치 출처 경고를 넣은 뒤에도 그레이마르 10턴 런 `99c6733d-6e17-432e-b778-634e083103c0`(`...brawler_postfix.json`)의 에드릭 대화에서 `열두 칸`, `열두 닢`, `열다섯`, `은전 몇 개`가 생성됐다. 세계관 어휘 감사의 탐지력을 높였지만 **수치 환각 자체는 막지 못했다**. 턴 조건부 서술 재시도는 추가 호출·지연이 생겨 별도 사용자 선택을 기다린다. 승인 전에는 호출을 추가하거나 수치를 기계적으로 삭제하지 않는다.
- P2/N-14: 노부인 런 T5의 `목소리 큰 생선상인: 조심하오.` 등 네 줄은 큰따옴표가 없어 기존 마커 적용률 분모에 포함되지 않았고, 화면에서는 실제 미표기 발화다. 감사기에 줄 시작 `화자: 발화` 후보를 **회색 수동 검토**로 추가했다. 같은 런 재감사에서 실제 위반 0/회색 4, 기존 따옴표 표본의 마커 적용률 28/29(96.6%)로 집계된다. 표제도 같은 형식이라 자동 위반으로 단정하지 않는다. 인라인 마커의 완료 본문 문단 분리와 스트림 화자 추출은 표적 단위 테스트가 있지만, 이 무따옴표 대사와 실제 브라우저 SSE·완료 화면은 여전히 통과하지 못했다.
- P3/N-05·06·09: 별도 연속 대화 런 `26161784-1eea-49ae-9f6e-1ba199327d4d`(`...brawler_free_input_postfix.json`)은 미렐라 8턴 고착, 같은 `임금 조작 패턴에 대해 묻는다` 선택지 재삽입, 감각어 반복, 영어 `Basket` 누출을 보였다(정본 V0~V14 11/15). 원인은 직전 선택지 중복 제거 **뒤에** `EmptyHandedChoice:STEER`가 동일 질문을 다시 넣는 순서다. 이는 관찰·원인 규명이지 수정/통과가 아니다. 생성 nano 선택지의 `사진으로 찍는다`는 별도 세계관 경계 위반이라, 명백한 현대 선택지 세트를 폐기해 서버 기본 선택지로 돌아가는 방어와 단위 테스트를 추가했다. 실모델에서 같은 출력이 재발하는 경로는 미검증이다.
- 최신 첫 공격 런은 정본 14/15로 V9의 `그대` 반복이 실패했다. 자동 점수와 별개로 첫 공격의 성공 서술이 모호하므로 P1 관문이 닫히지 않았다. **순서 규칙에 따라 P3의 구조 변경, P4~P5의 도입/결말 대규모 수정, P7 출시 판정은 이번 회차에 진행하지 않는다.** 다음 순서는 P1 성공·부분 성공 행동 정합과 N-11 레닉 첫 조우 → P2 정본 수치/무따옴표 발화와 실제 UI → P3 반복 질문·진전 → P4~P7이다. 각 관문의 재현 런/판정이 확보되기 전 상태를 완료로 올리지 않는다.
- P1 지시를 성공 시 **첫 두 문장에 공격·대상 접촉·반응**, 부분 성공 시 복합 행동의 각 핵심 동작 결과로 한 번 더 좁혔다. 최신 10턴 런 `4434c787-c9c6-4d5f-a4e5-115884f2c0ff`(`...first_fight_directive_retry.json`)에서 첫 두 공격은 `FREE_PLAYER_4/5`, 대상 `NPC_MIRELA`, SUCCESS로 안정적으로 라우팅됐고 정본 V0~V14는 15/15였다. 그러나 T4 본문은 주먹이 **약초 더미만 스쳤다**고 쓰고, T5는 **밀치기만** 확정하며 주먹 결과를 생략했다. 지시 강화만으로 행동-판정 일치를 보장하지 못한 반증이다. T6에는 마커가 붙은 발화자가 6명으로 P0의 최대 2명을 넘었다. 새 감사 규칙은 이 명백한 초과를 실제 위반으로 잡고, 같은 런의 `골드 몇 닢`과 합쳐 **실제 위반 2건**을 검출했다. 마커 적용률 27/27(100%)만으로 화자 품질을 통과 처리하지 않는다. **P1/P2 관문은 계속 실패.** 이 실패를 프롬프트 문구만 더 덧대거나 추가 LLM 호출을 묵시적으로 넣는 방식으로 덮지 않는다.
- 최종 로컬 검증: 서버 `pnpm build` PASS, 전체 Jest 201 스위트·2737 테스트/스냅샷 17건 PASS(기존 1스위트·2테스트 skip), Python 품질 감사 단위 8건 PASS, 로컬 3턴 스모크 PASS, root/server `git diff --check` PASS. 클라이언트 코드는 수정하지 않았으므로 빌드는 생략했다. 이번 변경은 여전히 **로컬 미커밋·미푸시**이며, 서버에는 미커밋 소스의 빌드가 실행 중이다. `/v1/version`이 HEAD `a17c4f3`만 반환하는 한 실제 배포 버전을 그 해시만으로 식별할 수 없다는 위험이 있다.
- 후속 설계 판단: P1은 명령형 프롬프트만으로 성공 공격의 실제 결과를 보장하지 못했다. 다음 구현은 기존 서버 판정과 플레이어 입력에서 **행동 핵심 동작·대상·확정 가능한 결과**를 구조화해 모델에 전달하는 계약을 먼저 설계하고, 서술이 이를 빠뜨리거나 반대로 쓴 경우를 계측해야 한다. 접촉 부위·상처·금액을 새 정본으로 만들지 않는다. 화자 2명 제한과 금지 화폐는 결정론적 감지가 가능하나, 복합 공격의 의미 누락은 단순 키워드만으로 안전하게 판정하기 어렵다. 위반 시 추가 서술 호출을 허용할지, 미검출/검출만 할지는 지연·비용과 함께 별도 결정한다. 사용자 결정 전에는 호출 증가·본문 강제 삽입을 시행하지 않는다.

## 10. 2026-09-25 진행 우선 회차 — 독립 단계 병행, 완료 판정 분리

- 사용자가 세부 항목에 과도하게 묶이지 말고 진행률을 높이도록 이번 건의 판단을 위임했다. §3의 관문을 **단계 착수 차단**이 아니라 **완료·출시 판정**에 적용한다. P1의 성공 공격 묘사와 P2의 무근거 정밀 수치·실제 UI 화자 검증은 여전히 미해결이다. 그렇다고 P3~P5의 독립적이고 안전한 수정을 막지 않는다. 공격 의미 누락을 문자열로 판정해 LLM을 자동 재호출하는 안은 오검출 가능성과 지연 증가에 비해 이득이 불명확해 이번 회차에는 채택하지 않았다.
- P3/N-05·06: `EmptyHandedChoice:STEER`가 직전 선택지 중복 제거 **후** 같은 질문을 다시 삽입하던 원인을 고쳤다. 현재 노드에서 완료된 최근 3턴의 `llmChoices` 라벨에 같은 질문이 있으면 강제 삽입하지 않는다. 동등 라벨의 자발적인 nano 생성까지 전부 막는 변경은 아니다. 코어 재현 테스트를 먼저 실패시킨 뒤 구현·통과시켰다. 그레이마르 채티 10턴 런 `c47ee31a-a8df-4437-b445-21876db58d43`(`playtest-reports/narrative_quality_20260925_compromise_graymar_chatty.json`)에서 T8의 에드릭 질문이 T10에 다시 나타나는 것을 확인해, 처음의 1턴 비교를 3턴으로 넓혔다. **이 런은 3턴 확장 전이므로 확장 효과의 실모델 재현 증거는 아니다.** 확장 후 서버로 돌린 극야해안 런에서 같은 질문 재삽입은 보이지 않았지만 동일 자극을 재현하지도 않았다.
- P5/N-16: 결말 프롬프트에 이미 서버가 확정한 `endingResult.arcTitle`과 `cityStatus.summary`만 `[확정된 결말 맥락]`으로 전달한다. 보상 예정액처럼 지급이 확정되지 않은 `arcRewards`는 끌어오지 않고, 미확정 증거·처벌·비용을 꾸며내지 않도록 한다. 표적 프롬프트 테스트와 블록 등록 테스트가 통과했다. **현행 버전으로 `arc_finale`까지 도달한 표본은 없으므로 P5 완료가 아니다.** 오래된 9/2 완주 런은 이 수정의 검증 근거로 쓰지 않는다.
- P4/P7 단기 확인: 그레이마르 신규 계정/채티 10턴 런은 자동 검증 12/15(V2 조우 다양성·V5 방문 기록·V14 대화 상한 경고), 품질 감사 실제/회색 0/0, 마커 31/31이었다. 로넨 의뢰 이후 에드릭 한 명을 오래 선택한 경로라, V14 경고만으로 서버의 강제 대화 고착으로 단정하지 않는다. 극야해안 신규 계정/채티 10턴 런 `fabfd538-734f-4dd5-a7cd-90cb9ae48bf6`(`playtest-reports/narrative_quality_20260925_compromise_star_sand_chatty.json`)도 12/15(V2 조우 다양성·V9 품질·V14 대화 상한 경고)였다. T1 의뢰 수락→T2 여관 재진입→이렌 대화→T9 여관 이탈→T10 부두 이동의 공간 연결은 읽힌다. 다만 신규 계정에서 같은 인물 위주로 8턴을 쓴 편향이 있어 P4의 전체 첫인상이나 P7 통합 통과로 과장하지 않는다.
- 극야해안 품질 감사는 실제 위반 2건(`느낀다` 메타 서술, 극야 DAY의 `햇살`)·회색 0건, 마커 27/27이었다. 첫 공통몽이 추락 전날 시작됐다는 문장 자체는 `FACT_SS_FIRST_DREAM`과 일치한다. 두 어휘 위반은 P6 후속 표면 작업으로 추적하되 이번 회차의 P3/P5 진행을 취소할 중대 정합 결함은 아니다. 그레이마르 감사 0건도 이전 공격/정밀 수치의 미해결을 뜻없이 덮지 않는다.
- 작업 기준 HEAD는 root `367e02c`, server `a17c4f3`, client `9254daa`. 이전 회차 변경을 포함한 root/server 수정은 로컬 미커밋이며 클라이언트 추적 코드는 건드리지 않았다. 이번 회차의 서버 실런은 미커밋 소스를 빌드해 launchd 서비스로 재시작한 로컬 버전에서 수행했다. `/v1/version`의 `a17c4f3`은 HEAD 표기일 뿐 dirty 빌드의 내용 해시가 아니다. 원격은 이 변경을 받지 않았다.
- 최종 회귀: 서버 `pnpm build` PASS, Jest 201 스위트·2739 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip). 처음 전체 테스트에서 새 결말 프롬프트 헤더가 누출 제거 목록에 빠진 것을 잡아 등록 후 재실행했다. Python 품질 감사 단위 8건 PASS, 콘텐츠 감사 ERROR/WARN 0, 로컬 서버 재시작 후 3턴 스모크 PASS, root/server `git diff --check` PASS. 브라우저의 스트리밍/완료 UI와 현행 피날레 완주는 이 회차에서 검증하지 않았다.

## 11. 2026-09-26 커밋·푸시와 후속 표적 검증

- 첫 배포 묶음: root `2ae8787`·`3bc6c4f`, server `691270f`·`9839e33`을 각각 `origin/main`에 푸시했다. 기존 root의 JEV 미추적 문서·실험 코드와 client의 주사위 시안은 범위 밖이라 그대로 보존했다. 서버는 빌드 후 launchd로 재시작했고 `/v1/version`의 `9839e33`과 3턴 스모크 PASS를 확인했다. 전체 Jest 201 스위트·2741 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip), Python 감사 단위 9건 PASS.
- 독립 검토 — **Standards:** 감사기의 새 `젓가락`·화폐 단어 패턴에 오른쪽 경계가 없어 임의 합성어를 실제 위반으로 오판할 수 있었다. 허용 조사와 `동전주머니`·`닢짜리`만 명시하고 경계를 확인하도록 고쳤다. 라벨 문자열을 STEER 질문의 식별자로 쓰는 설계 취약점은 구조화 토픽/인물 키가 없는 현 배선의 잔여 개선점이다. **Spec:** P3의 사실·관계 진전, P5 현행 완주, N-14 실제 UI 확인은 아직 부분/미검증이다. 공격 성공을 곧 제압 성공으로 지시하던 과잉 해석과, 현대 물건이 섞인 선택지 하나 때문에 정상 후속 선택지까지 버리던 방어는 즉시 수정했다. 전자는 서버가 확정한 성과만, 후자는 금지 선택지만 제거하고 2개 미만일 때만 폴백한다.
- P1 표적 런 `f8320547-df18-43cf-bb18-b4b455909fe7`(`playtest-reports/narrative_quality_20260926_fight_review_live.json`)은 10턴 중 지목 공격 T4 `PARTIAL`/T5 `SUCCESS`가 모두 `FREE_PLAYER`·`NPC_MIRELA`로 갔다. T5 본문은 밀치기와 이어진 주먹의 팔뚝 접촉, 상대의 물러남을 명시해 이전의 주먹 생략을 재현하지 않았다. 다만 T4는 빗나간 주먹과 바구니 접촉으로 부분 성과를 읽을 수 있지만 첫머리의 불필요한 감각 문단이 길다. 정본 자동 검증은 14/15(V14 동일 NPC 상한 경고), 품질 감사 실제 위반 1건(`골드 세 닢`), 마커 19/20. 이 한 런만으로 P1 전체 완료나 대사 UI 통과를 선언하지 않는다.
- P3 연속 대화 런 `4ee90711-a10e-4888-ac23-e4dc3371bb18`(`playtest-reports/narrative_quality_20260926_repeat_choice_live.json`)은 레닉과 8턴 대화하며 T10 `장부의 조작 흔적을 묻는다` 뒤 T11 `장부의 조작 흔적에 대해 더 묻는다`가 재등장했다. 자동 검증 12/15(V2·V5·V14), 품질 감사 0건·마커 38/38. 동일 인물 고착은 에이전트가 그 인물을 명시적으로 선택한 턴과 섞여 있어 V14 경고만으로 강제 고착이라 단정하지 않는다. 이 런 이후, 일반 nano 중복 제거도 현재 노드의 최근 3턴 최종 선택지와 비교하고 위 두 장부 질문을 동등하게 보는 최소 정규화를 추가했다. 질문과 실제 조사는 구분하는 단위 테스트를 보존한다. **이 새 배선은 아직 실서버/실모델에서 같은 자극으로 재검증 전이다.**
- P5 현행 `arc_finale` 완주와 N-14 브라우저의 스트리밍·완료 화면은 여전히 남아 있다. P2의 화폐 단위 위반과 근거 없는 정밀 수치도 전역 프롬프트 한 번으로 해결됐다고 보지 않는다. 이들은 추가 모델 호출을 자동으로 늘리지 않은 상태에서 별도 표적 검증·수정 대상으로 유지한다.
- 두 번째 배포 묶음: 최근 3턴 일반 nano 선택지 중복 제거와 질문 표현 정규화를 server `a291f5e`, 위 표적 근거를 root `1991da2`로 커밋·푸시했다. 서버 `/v1/version` `a291f5e` 일치, 전체 Jest 201 스위트·2743 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip), 빌드·재시작·3턴 스모크 PASS. 두 턴 전 `장부의 조작 흔적을 묻는다`와 현재 `장부의 조작 흔적에 대해 더 묻는다`는 중복으로, 같은 장부의 **조사**는 별도 행동으로 유지하는 단위 테스트가 통과했다. 이 커밋 이후 동일 자극의 10턴 실런은 아직 수행하지 않았다.
- N-14 클라이언트 공개 경계에서 인라인 `@[무명 인물]` 마커가 지문 뒤에 붙은 완료 본문을 독립 말풍선으로 렌더하고, SSE `done` 버퍼가 해당 마커와 `explicit-v1` 귀속 정책을 보존하는 테스트를 추가했다(client `0209352`). 클라이언트 전체 Vitest 9파일·75테스트 PASS, Webpack 프로덕션 빌드 PASS. 기본 Turbopack 빌드는 처음엔 Google Fonts 접근 차단, 네트워크 허용 재시도에서는 샌드박스의 포트 바인딩 금지로 실패했다. **실제 브라우저의 진행 중/완료 화면 검증을 대체하지는 않는다.**
- P5 현행 완주 검증을 위해 HERBALIST 30턴 런을 시작했지만, 새 테스트 계정 50p가 사전 필요량 150p에 못 미쳐 런 생성/턴 소비 전에 중단됐다. 테스트 포인트 원장 변경은 임의로 하지 않고 사용자의 선택을 요청했다. 현재 확보한 증거는 결말 프롬프트·서버 생성기 단위 계약까지다.
- P3 수정판 10턴 런 `52a2c449-2c74-4b25-a257-934db851aac8`(`playtest-reports/narrative_quality_20260926_repeat_choice_postfix.json`)은 같은 에드릭에게 T7 `날카로운 회계사에게 장부 조작 흔적에 대해 묻는다`와 T8 `에드릭 베일에게 장부 조작 흔적에 대해 묻는다`를 연속 노출했다. **호칭이 바뀌면 문자열 유사도만으로는 중복을 잡지 못하는 반증**이다. 후속 코어는 동일 `sourceNpcId`·TALK의 `에게 + 질문` 부분을 비교하고, 반대로 다른 NPC에게 같은 문구로 묻는 새 접근은 유지한다. 두 경우 모두 단위 테스트를 실패시킨 뒤 수정·통과시켰다. 이 추가 변경의 동일 자극 실런은 아직 없다. 런 자체는 자동 검증 12/15(V2·V5·V14), 품질 감사 실제 1건(대사 내부 영어 `places`)·마커 24/28(85.7%)이었다. 어휘·화자 표면은 P2/P6에 남긴다.

## 12. 2026-09-26 후속 회차 — 도입 동기와 실런 판정 정리

- 시작 기준: root `0b4b952`, server `9eb1f9d`, client `0209352`. 세 레포의 추적 파일은 시작 시 깨끗했고, 기존 미추적 JEV 자료와 주사위 시안은 건드리지 않았다. 로컬 `/v1/version`은 `9eb1f9d`로 확인했다.
- P7 검증기 회귀: 첫 그레이마르 채티 10턴 `bd4a80a3-c859-42d3-bf6d-cff9521b75ca`(`playtest-reports/narrative_quality_20260926_alias_followup.json`)는 현재 런의 CORE 최장 연속 화자가 4턴으로 상한 이내였지만, V14가 **과거 서버 버전의 상한 초과 2건**을 합산해 14/15로 표시했다. 누적 창을 서버 해시·기동 시각, 저작 팩 JSON, 공개 LLM 설정 전체(모델·fallback·temperature 등), 현재 런타임 플래그, 테스트 설정이 같은 런으로 제한했다. 기동 시각으로 같은 HEAD의 재시작 전후도 분리하므로 서버 캐시 콘텐츠/미커밋 빌드가 섞일 위험을 줄인다. 해시나 설정을 확인할 수 없는 런은 단독 계측한다. 이전 원장은 삭제하지 않았고, 이전 행에 비교군 정보가 없으면 새 창에서 제외한다. `scripts/test_playtest_gate_ledger.py`의 5개 사례는 서버 버전·기동 시각·콘텐츠·모델/교차 노브·테스트 설정 변경 및 불명 비교군 격리를 확인했다. V12·V13·V14에 동일하게 적용된다. 단, 한 프로세스의 실행 중 관리 설정이 바뀌는 시점이나 디스크의 콘텐츠가 서버 캐시와 달라진 상태는 런 도중 재조회하지 않으므로 보수적으로 해석한다.
- P3: 위 런과 수정 후 두 번째 그레이마르 채티 10턴 `d4cfa747-72dd-443a-8777-7b342c644eea`(`playtest-reports/narrative_quality_20260926_intro_followup.json`)은 관리인에게 최대 4턴 연속 대화한 뒤 경비대로 이동했다. 별칭→실명으로 같은 질문을 다시 제안하던 **동일 자극은 나오지 않았으므로** 서버 `9eb1f9d`의 중복 제거를 실모델 PASS로 판정하지 않는다. 첫 런 품질 감사 0/0·마커 24/24, 두 번째 0/0·20/20이었다.
- P4: 기존 10턴 수락 본문은 로넨의 개인적 위험 없이 장부·길드 체면만 말했다. `graymar_v1/scenario.json` 수락 지시에 이미 정본인 “도난 보고 전 그를 찾는 이들”을 짧은 발화로 환기하도록 추가했다. 첫 10턴 재검증은 로넨이 문을 보는 행동만 보여 지시를 좁혔고, 1턴 `39d5bb5e-c878-475c-802d-ccaad8b7f088`(`playtest-reports/narrative_quality_20260926_acceptance_recheck.json`)은 그가 자신을 찾는 이들 때문에 장부를 서둘러야 한다고 직접 말했다. 추격자 정체·처벌·보상은 추가하지 않았다. 단일 첫 턴 검증이므로 P4 전체 완료가 아니다.
- P7: 도입 지시 수정 전 두 번째 그레이마르 10턴은 정본 자동 15/15, 당시 동일 서버 해시 2개 실런의 CORE 초과 0건, 품질 감사 실제/회색 0/0이었다. 이후 **당시 비교군 기준 동일 조건** 그레이마르 채티 10턴 `e6ef2646-9e5e-4850-a3fa-b9f85ce0b97b`와 `173e431a-da33-4429-b296-884b7a16bfc7`(`playtest-reports/narrative_quality_20260926_cohort_chatty_{a,b}.json`)은 각각 자동 15/15, V14 최장 4턴·CORE 초과 0건이었다. 최종 비교군 지문으로는 재계측 전이다. 자동 점수와 달리 품질 감사에서 두 런 모두 화폐 표현 실제 위반 2건씩(`은전`, `닢`, `동전`)을 찾았다. 마커는 26/26·20/20. 최종 로컬 회귀는 서버 Jest 201 스위트·2747 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip), 서버 빌드 PASS, 클라이언트 Vitest 9파일·75테스트 및 Webpack 프로덕션 빌드 PASS, 콘텐츠 감사 ERROR/WARN 0, Python 품질 감사 9건·원장 테스트 5건 PASS. 별칭 전환의 동일 자극, 현행 극야해안 후속 런·피날레는 아직 없다.
- P2/N-19: 두 번째 동일 비교군 런 T4에서 nano 선택지 `날카로운 회계사에게 50 크레딧을 건네며 더 많은 정보를 요구한다`가 실제 노출됐다. 생성 선택지에서만 현대 화폐 선택지를 제거하고 나머지 장면 선택지는 유지하는 실패→통과 단위 테스트를 추가했다. 자유 입력의 `크레딧이 무엇인지 묻는다`가 세계 밖 행동으로 오판되지 않는 것도 실패→통과 테스트로 확인했다. 이 방어를 적용한 **동일 출력의 실모델 재발 검증은 아직 없다.** 따라서 N-19는 완화이며, 본문에 생기는 근거 없는 화폐·정밀 수치(N-08)는 별도 미해결이다.
- P7 극야해안 후속: 서버 `aa976cb` 재시작 후 같은 최종 비교군 `6eb960c3ffbfd870`으로 순례자/채티 10턴 `770b630f-7523-4bfc-9a81-b203e2f55e50`, `f9b37f15-b21a-4147-a924-be2509c89b52`(`playtest-reports/narrative_quality_20260926_star_sand_cohort_{a,b}.json`)를 실행했다. 각각 V0~V14 자동 15/15이며 두 번째는 V12~V14 **2런 누적 표본**으로 판정됐다. 정본 품질 감사 실제/회색 0/0, 마커 24/24·25/25. 둘 다 수락→여관→이렌 대화→여관 이탈→부두 진입으로 이어졌고 두 번째 T6은 “추락 전날 첫 공통몽, 잠든 뒤 사흘간 깨어나지 못함”의 정본 순서를 보존했다. 반면 수락 직후 여관으로 돌아오는 T2 본문은 이유를 직접 말하지 않는다(선택지 `hubHint`에는 숙박 장부·실종 손님의 방이 명시됨). 따라서 N-03/P4는 완화이지 완료가 아니다. 두 런 모두 10턴 초반·사교 성향에 편중돼 극야해안 전체 또는 출시 통과 근거로 쓰지 않는다.
- N-03 표적 실험: 수락 본문을 여관 문턱에서 멈추게 하면 즉시 재진입이 자연스러워질 것이라 가정하고 로컬에서만 콘텐츠 지시를 바꿔 2턴 `c29d1397-6a96-457b-95e2-da4a95fc6a1b`(`playtest-reports/narrative_quality_20260926_star_sand_accept_threshold.json`)를 실행했다. T1은 이렌이 문턱에 선 것으로 화자를 바꿔 썼고, T2는 여전히 갈림길에서 여관으로 걸어오는 이동을 생성했다. **가설이 반증되어 콘텐츠 변경을 전부 되돌리고 서버를 재시작했다.** 2턴의 자동 11/15는 V2·V4·V5·V6 장기 표본 부족이라 통합 점수로 해석하지 않는다. 다음 수정을 한다면 수락→허브→동일 건물 선택의 출발 위치/목적을 이동 서술 입력에서 일관되게 연결해야 한다.
- P2/N-14: 로컬 클라이언트의 `/play` 로그인 화면까지 브라우저로 열어 페이지 진입은 확인했다. 인증된 게임 턴을 진행하지 않아 실제 스트리밍/완료 말풍선은 **미검증**으로 유지한다. 기존 클라이언트 단위 테스트가 이를 대체하지 않는다.
- P5(당시 상태): 새 테스트 계정 기본 50p로 30턴 필요량 150p를 감당할 수 없고, 정본 테스터 계정 잔액도 0p로 읽기 확인했다. 이후 사용자 승인으로 테스트 전용 100p를 1회 발급해 아래 §13의 장기 런을 실행했다. 완주 여부는 그 결과로 판정한다.

### N항목 현행 분류 (출시 완료 판정과 별개)

| 항목 | 상태 | 현행 근거·남은 확인 |
|---|---|---|
| N-01 | 보존 | 두 팩 단기 런에서 사건형 출발 유지; 첫인상 전체 판정은 P7 대기 |
| N-02 | 완화 | 로넨 수락의 개인적 위험을 정본 발화로 1턴 확인; 다른 프리셋 대기 |
| N-03 | 완화 | 극야해안 여관 재진입 동선이 단기 런에서 읽힘; 여러 선택 경로 대기 |
| N-04 | 미해결 | 이동 본문이 여전히 긴 사례가 있어 진전 대비 길이 재검토 필요 |
| N-05 | 완화 | 최근 3턴 중복 제거와 후속 선택지 보존 테스트; 범용 메뉴 빈도 동일 분모 비교 대기 |
| N-06 | 완화·미완료 | 발견한 같은 fact의 재질문에서 무관 fact 오공개를 차단; 본문이 답을 회피하는 실모델 잔여 사례 있음 |
| N-07 | 미해결 | NPC별 말투 보존, 반응 기능 다양성은 대표 런 추가 정독 필요 |
| N-08 | 완화·미완료 | 에드릭의 매번 새 수치 강제 저작을 제거했고 최종 표본의 무근거 정밀 금액은 0건; 다른 출처·화폐 경로는 미검증 |
| N-09 | 미해결 | 감각 묘사 선행·반복이 이번 런에도 보여 문장 리듬 통과 전 |
| N-10 | 판단 보류 | 회계사 장면에 악사 선택지가 붙는 동일 상황 현행 재현 미실시 |
| N-11 | 판단 보류 | 레닉 첫 조우의 경비대 입구→외곽 술집 연결 동일 상황 미실시 |
| N-12 | 완화 | 지목 공격 라우팅·성공 접촉을 단기 런에서 확인; 다른 판정 확장 대기 |
| N-13 | 현행 미재현 | 이렌 정본 및 극야해안 후속 10턴 2회에서 잠/각성 역전 미발견 |
| N-14 | 미해결 | 완료·SSE 단위 테스트는 통과했으나 30턴 런 79/80 마커·한 턴 NPC 3명 발화; 실제 화면 미검증 |
| N-15 | 미해결 | 영어 `places` 등 과거 표면 누출, 전체 대표 런 무오류 입증 전 |
| N-16 | 완화·미완료 | 고발 노선 명시 피날레 2회 확보; 지급·정적 에필로그 정합 수정, 다른 루트/팩과 본문 체감은 미검증 |
| N-17 | 완화 | 오유도 선택지 수정·별도 단기 런 재검증; 다른 단서 경로 대기 |
| N-18 | 완화 | JSON형 본문 방어 후 이번 두 런에 재발 없음; 두 팩 확대 대기 |
| N-19 | 완화 | 생성 `50 크레딧` 선택지 노출 재현 후 경계 필터 단위 테스트; 수정 후 동일 출력 실런 대기 |
| N-20 | 해결 확인 | 엔딩 분기 미정의 `status` 참조 제거 후 자동 종료·명시 피날레 실런 모두 서버 500 미재발 |
| N-21 | 해결 확인 | 커밋된 저작 아크의 incident 자동 종료를 보류한 뒤 `arc_finale` 선택 실런 완료 |

## 13. 2026-09-26 피날레 장기 런 — 표본 미도달·종결 오류 발견

- 사용자 승인 범위 안에서 신규 테스트 계정의 기본 50p에 **1회 사용·45분 만료 100p 코드**를 적용해 총 150p로 시작했다. 결제·일반 계정 조정은 하지 않았다. `playtest.py`에서 경로 커밋 뒤 `arc_finale`도 우선 클릭하도록 선택기와 2개 단위 테스트를 추가했다. 기존 `arc_` 1회 게이트는 커밋 후 결말을 놓칠 수 있었다. 이 런에서 결말 선택지는 실제 노출되지 않아 새 선택기의 실런 통과를 주장하지 않는다.
- 그레이마르 HERBALIST 비사교 자동 30턴 `02cda00d-7cc4-461a-8cc5-91cff6a7634d`(`playtest-reports/narrative_quality_20260926_finale_herbalist30.json`)은 성공 제출 29턴과 서버 500 한 번이다. 실행 당시 하네스 V0~V14는 15/15였으나 **RUN_ACTIVE·S1_GET_ANGLE로 끝나 `arc_finale`는 0회**였다. `FACT_WAGE_FRAUD_PATTERN`은 얻었지만 S2에 함께 필요한 `FACT_TAMPERED_LOGS`가 없어 아크 진행이 멈췄다. 이는 목적 있는 완주 경로의 불가능을 입증하지 않고 이 자동 경로의 미도달만 확인한다. 30턴 자동 점수나 표본 부족으로 통과 처리된 V12~V14를 피날레 성공으로 읽지 않는다. 서버 500이 있어도 당시 V0가 29턴 성공만 세어 PASS로 표시한 결함도 발견했다. 이후 V0는 턴 제출 실패가 1건이라도 있으면 FAIL하고 `submitErrors`를 리포트에 보존하며 실패 런을 후속 누적 판정 표본에서 제외하도록 수정·4개 단위 테스트를 추가했다. 기존 저장 리포트의 15/15를 소급 변경하지 않는다.
- 23번째 입력(실제 turnNo 30)에서 엔딩 조건 분기가 실행됐으나 `LocationTurnService.handleLocationTurnInner`가 정의되지 않은 `status`를 `finalizeRunEnding`에 넘겨 500을 냈다. 포인트 5p는 환불됐고, 런은 계속 활성 상태로 남았다(N-20). 사용하지 않는 인자를 양쪽 호출부/타입에서 제거했다. 이 시점에는 서버 전체 테스트·빌드 뒤 같은 엔딩 조건의 실런 재현이 필요했다(후속 결과는 §14). 테스트 계정 잔액은 종료 시 5p였으며 추가 발급은 하지 않았다.
- 정본 품질 감사 실제 위반 2건(본문 `금화`, 한 턴 NPC 3명 대사), 회색 0건, 마커 79/80(98.8%). 자동 검증 15/15와 달리 P2/P6/P7 표면 관문도 통과하지 못했다. P5는 현행 `arc_finale` 본문의 상태-서술 일치 판정까지 진행되지 않았다.

## 14. 2026-09-26 목적 경로 피날레 재검증 — 결말 도달, 출시 보류

- 시작 기준 root `2557833`, server `e44404d`, client `0209352`; 세 레포 추적 파일은 깨끗했고 로컬 서버 `/v1/version`은 `e44404d`였다. 테스트 전용 기존/신규 `@test.com` 계정만 사용했다. 이 회차에 1회용 150p 코드로 신규 계정 기본 50p를 200p로 만들고, 남은 같은 계정에 부족분 100p만 다시 발급했다. 세 실런 종료 후 잔액은 35p다. 일반 계정과 결제에는 손대지 않았다.
- 무작위 장소 순환의 피날레 미도달을 구별하기 위해 `scripts/playtest.py --finale-route expose-corruption`을 추가했다. 실제 플레이어의 조사 입력·이동·노선 선택·3막 행동·`arc_finale`만 제출하며 fact나 상태를 주입하지 않는다. 루트 3막의 장소는 콘텐츠에서 읽고, 새 경로 선택 테스트 4건이 통과한다. 기존 테스트 계정을 재사용하는 `--account-email`은 `@test.com`으로 제한했다.
- 첫 목적 런 `3e5bc8af-07e6-42fb-a6df-a18e72e77db2`(`playtest-reports/narrative_quality_20260926_finale_expose_directed.json`)은 17회 제출/서버 turnNo 22, T7 고발 노선 커밋→T17 `arc_finale`→RUN_ENDED, 500 재발 없음. 자동 V0~V14는 15/15이나 감사 실제 1건(`은화`), 마커 44/44. 상태는 S3지만 3막 완료·`FACT_OFFICIAL_INQUIRY` 획득으로 명시 결말이 열렸다. 완료 화면의 실제 의뢰 정산은 60골드, 최종 소지금 230골드인 반면 지급 배선 없는 콘텐츠 루트 보상 50골드·평판 +10이 별도 배지로 노출되고, 정적 에필로그가 확인되지 않은 장교 연행·밀수 조직 와해를 단정했다.
- 서버가 미지급 `arcRewards`를 새 엔딩 응답에 넣지 않도록 하고, 클라이언트가 과거 응답의 배지도 표시하지 않도록 했다. 지급 확정된 `questRewardGold`/`finalGold`는 유지한다. 그레이마르 고발·안정 에필로그는 공식 조사 개시와 미결 처분으로 좁혔다. 초기화된 40여 명 전원을 늘어놓던 인물 에필로그는 **이름이 공개된 인물**로 한정했다. 서버/클라이언트의 완료 화면 경계 테스트를 추가했다. 상세 서술에 미확정 원본·후속 결과가 끼는 위험은 여전히 남아 있다.
- 수정판 두 번째 런 `2fe53aeb-f25b-4d26-a97e-779c179ef156`(`...finale_expose_postfix.json`)은 17회 제출/turnNo 22, T9 커밋 후 T17 세 번째 막에서 사건 전체 해결 판정이 먼저 RUN_ENDED를 만들어 **명시 결말 선택을 건너뛰었다**. 이전 N-20의 500은 재발하지 않았으나 별도 종결권 회귀다(N-21). 저작 팩에서 명시 커밋한 런은 일반 incident 종료를 보류하고 `arc_finale`를 선택해야 끝나도록 회귀 테스트를 실패시킨 뒤 수정했다. AUTONOMOUS 팩의 독립 종결 규칙과 무커밋 자연 종료는 보존한다.
- 세 번째 런 `23f2f9d5-2a88-403f-bd96-08f31d44e1ab`(`...finale_explicit_postfix.json`)은 19회 제출/turnNo 24, T9 커밋→T18 세 막 완료→T19 `arc_finale` 클릭→RUN_ENDED. 신규 에필로그·60골드 정산·최종 230골드 확인, 미지급 `arcRewards` 없음, 인물 에필로그는 공개된 로넨·에드릭 2명. 자동 14/15(V9 반복 `사람`), 정본 감사 실제 3건(`닢` 3회)·마커 50/51(98%). **피날레 선택권과 금액 표시는 개선됐지만 P2/P6/P7 품질 관문은 여전히 불합격**이다. 본문은 고발 선택이 누구를 어떻게 바꿨는지보다 마이렐과의 작별·감각 묘사에 치우쳤고, 중요 인물 벨론은 끝내 이름이 공개되지 않아 인물 에필로그에 나오지 않는다. 다른 루트와 극야해안 결말, 인증된 브라우저 스트림/완료 화면을 이 표본으로 대신하지 않는다.
- 변경 검토에서 S5 안전망이 세 막을 모두 마치기 전에 `arc_finale`를 열 수 있는데도 안정·고발 에필로그가 공식 조사 착수를 단정하는 경우를 발견했다. `FACT_OFFICIAL_INQUIRY`가 실제 확정된 경우에만 기존 에필로그를 쓰고, 없으면 조사 여부가 미확정인 대체 문단을 사용한다. 조기 결말 회귀 테스트는 통과했지만 안전망의 **실런**은 아직 없으며, 다른 노선·극야해안의 정적 결말 단정은 P5 잔여 위험이다.
- 최종 회귀: 서버 Jest 202 스위트·2752 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip), 서버 빌드 PASS; 클라이언트 Vitest 10파일·76테스트 PASS, Webpack 프로덕션 빌드 PASS. 기본 Turbopack 빌드는 샌드박스의 포트 바인딩 금지로 중단돼 제품 빌드 결함으로 단정하지 않는다. 콘텐츠 정합 감사 전체 팩 ERROR/WARN 0, INFO 36; Python 경로 선택·기존 하네스·품질 감사 단위 테스트 24건 PASS. 테스트 통과는 금지 화폐·화자·결말 체감의 잔여 결함을 덮지 않는다.

## 15. 2026-09-26 P3 연속 재질문 표적 개선 — 서버 판정은 안정, 본문은 부분 개선

- 시작 고정점은 server `74e3974`·root `8421332`이며 추적 파일은 깨끗했다. 기존 root 미추적 JEV 자료는 보존했다. 기존 `@test.com` 전용 계정에 부족분 65p와 70p를 각각 1회용·2시간 만료 코드로 보충해, 같은 입력의 두 팩 기준선 10턴·수정판 10턴·최종판 7턴을 실행했다. 일반 계정·결제에는 손대지 않았다. 자동 V0~V14는 본문의 문답 모순을 검출하지 못하므로 정독·서버 `questReveal`을 주판정으로 삼았다.
- 기준선 그레이마르 `3fd3d809-0404-4eba-8a29-214ce19eff78`(`...edric_repeat_baseline.json`): 에드릭에게 같은 장부 조작 질문 네 번. T4는 맞는 `FACT_TAMPERED_LOGS`였으나 T6 `FACT_WAGE_FRAUD_PATTERN`, T7 `FACT_INSIDE_JOB`은 질문과 무관한 `matchedByTopic=false` 공개였다. 원인은 발견 fact를 키워드 검색에서 제외한 뒤 `매칭 없음`으로 해석해 다음 미발견 fact에 순서 fallback한 것, 그리고 “~에 대해 묻는다” 행동 서술형 질문을 `isQuestionInput`이 놓쳐 `INVESTIGATE` 확률 fallback이 열린 것이다.
- 기준선 극야해안 `2e3a6f58-4dcf-435b-a145-5d88539cca31`(`...iren_repeat_baseline.json`): 이렌의 첫 공통몽 시점은 T4 `FACT_SS_FIRST_DREAM`으로 이미 확인됐지만 T7은 시점을 말할 수 없는 듯 응답했다. 기존 `[이미 나눈 대화 — 반복 금지]`가 재질문에도 예외 없이 적용되는 반면 `질문 우선`은 되묻기·회피를 허용했다.
- 수정: fact 선택은 **발견분도 주제 매칭**해 동일 주제의 무관 fallback을 차단하고, 행동 서술형 질문을 질문으로 인식한다. 이미 발견했고 **그 NPC가 이전 턴에 직접·간접 공개한** fact를 재질문할 때만 정본 설명·NPC 버전·공개된 다음 힌트를 프롬프트에 주입해 직접 확인 답변을 요청한다. 최초 공개 턴·비질문·다른 NPC·관찰 출처에는 주입하지 않는다. 에드릭 `speechStyle`/`signature`의 “매번 다른 구체 수치” 강제를 제거하고, 정본 수치와 미확인 추정을 구분하도록 바꿨다. 일반 질문이나 새로운 fact 공개 경로의 거절/부분 답변 허용은 보존한다.
- 첫 수정판 그레이마르 `0d2167da-7429-443b-9119-20f34d2b95dc`(`...edric_repeat_postfix.json`)는 T4 정답 뒤 T5~7의 무관 fact 공개가 0건이었지만, 저작 말투 지시가 남아 T6 “아홉 푼” 같은 근거 없는 수치가 본문에 나왔다. 첫 수정판 이렌 `2fe6de98-18bb-413b-b3a6-e17d82b5d137`(`...iren_repeat_postfix.json`)은 정본 블록이 실제 프롬프트에 있었어도 T5·7 시점을 흐렸다. 이에 위 저작 지시를 수정하고 **이미 알려진 재질문**의 `질문 우선`에서 되묻기·회피 허용을 제거했다.
- 최종 7턴 그레이마르 `975dd69b-56a2-49ce-9891-aa7f66be5f64`(`...edric_repeat_final.json`): T4의 정본 fact 이후 T5~7 `questReveal=null`, 무근거 정밀 금액 0건. T5와 T7은 셋째 칸 넷째 줄·덧쓴 필체를 직접 확인하고, T7은 경비대/상단 윗자리의 실제 후속 방향을 대사에 제시했다. 그러나 T6은 엄격한 질문 우선 프롬프트가 실제 주입됐어도 첫 대사를 되묻기로 시작했다. 선택지는 T4 장부 관찰처럼 장면 고유 행동이 있으나, T5의 장부 소유/신체 접촉 가정이나 T6 공감·의심처럼 범용/어색한 후보도 남았다. 자동 14/15(V2 표본/조우), 정본 품질 감사 실제/회색 0/0·마커 13/13.
- 최종 7턴 극야해안 `f90b6c7e-e07b-4486-8236-ecf11df3744a`(`...iren_repeat_final.json`): T4 `FACT_SS_FIRST_DREAM`, T5~7 새 fact 0건. T5와 T6은 “추락 하루 전날 밤”을 명시하고 같은 문장이라는 다음 주제로 옮겼다. 그러나 T7은 이미 알려 준 시점/순서를 다 말할 수 없다는 식으로 다시 후퇴했다. T5의 장부 날짜 확인·같은 문장 질문은 후속 행동이지만 T4의 거래·보고 선택지는 범용이었다. 자동 13/15(V2·V6 표본 부족), 품질 감사 실제/회색 0/0·마커 14/14. 감사 0건은 답변 모순이 없다는 뜻이 아니다.
- **판정:** N-06은 무관 fact 오공개 해결·답변 회피 완화, N-08은 에드릭의 저작 수치 강제 제거 및 이 표본의 정밀 금액 재발 없음. 둘 다 P3 전체 통과는 아니다. 다음 분리 과제는 ① 반복 질문에 모델이 정본 확인을 거부하는 턴의 원인(다른 프롬프트 블록·이전 대사·후처리)을 계측하고, ② 새 fact 또는 명확한 인계가 있는 턴의 선택지가 실제 대상/장소로 가는지 동일 분모로 비교하는 것이다. 두 7턴 표본의 자동 점수를 이전 10턴과 수치 비교하지 않는다.
- 독립 검토가 두 안전 경계를 추가로 지적했다. 첫째, `discoveredQuestFacts`는 **런 전체**의 발견이지 현재 NPC의 과거 공개가 아니다. 재질문 정본 블록은 `discoveredQuestFactMeta`에 같은 NPC의 **이전 턴 직접·간접 공개 출처**가 있고 현재 판정이 FAIL이 아니며 현재 유효 태도가 HOSTILE이 아닐 때만 구성한다. 관찰 출처와 공개 방식이 없는 구형 메타는 강제 답변하지 않는 보수적 폴백이다. 같은 NPC 관찰/구형 메타·다른 NPC/이벤트 출처·현재 FAIL/적대 태도를 거부하는 테스트와 실제 사실 발견 시 공개 방식 저장 테스트를 추가했다. 이렌 최종 실런의 T4는 `observe`였으므로, 해당 런 T5~7의 재질문 정본 주입·응답 개선은 **현재 보수적 게이트를 적용한 재현 근거가 아니다**. 둘째, 최종 이렌 T6 생성 선택지는 미발견 `FACT_SS_SAME_WORDS`의 정확한 문장 “눈이 아직 바다를 보고 있다”를 먼저 노출했다. 서술 프롬프트에는 그 문장이 없고 런의 발견 목록에도 첫 공통몽만 있었다. 최종 nano 선택지 확정 전에 **미발견 fact 설명의 긴 정본 인용문과 동일한 생성 라벨만** 제거하고, 행동 선택지가 둘 미만이면 서버 기본 선택지로 복귀한다. 저작 선택지는 건드리지 않는다. 실제 극야해안 콘텐츠와 해당 라벨을 넣은 회귀 테스트는 통과했다. 이는 인용문 누출의 좁은 방어이며 의미만 바꾼 패러프레이즈나 선택지의 근거 없는 소품·행동을 전부 검증하는 장치는 아니다. 이 보강 이후 동일 자극 실모델 재발 검증은 아직 없으므로 P3 통과로 올리지 않는다.
- 위 안전 경계 적용 직후 이렌 7턴 `c2b3d416-0c8a-4945-973e-4a87e44cd5d8`(`...iren_repeat_provenance_final.json`)에서 첫 공통몽 단서는 이벤트 경로(출처 메타에 `npcId` 없음)로 발견됐다. T4·T6은 추락 전날을 말하지만 T7에 이렌이 다시 전후 순서를 모르겠다고 답했다. **NPC 공개 여부를 보수적으로 가린 것만으로 문답 연속성이 해결되지 않는다.** 따라서 NPC 과거 발언으로 단정하지 않으면서, 플레이어가 관찰·기록으로 이미 확인한 정본을 별도 블록으로 전달했다. 이 경로는 답변을 강제하지 않고 NPC의 거절·유보를 허용하지만, 서술이 플레이어의 객관적 시점을 뒤집거나 새 단서로 재공개하지 못하도록 한다. 이벤트/관찰 출처·미발견·이미 직접 공개한 사실의 분기 및 프롬프트 계약은 단위 테스트로 검증했다.
- 최신 이렌 7턴 `5208819a-1921-4c5f-b92b-fbd3f596d1a1`(`...iren_repeat_player_known_final.json`)은 T4 첫 공통몽을 새 메타 `NPC_SS_IREN/direct`로 기록했고 T5~7의 실제 저장 프롬프트에 `[이미 확인한 사실에 대한 재질문]`이 들어갔다. 네 질문 모두 본문에서 추락 전날 밤을 유지했고 전후 순서 번복·미발견 정본 인용문 선택지 재노출은 없었다. 이는 **직접 공개 경로의 실런 검증**이지 이벤트/관찰 출처용 신규 블록의 실런 검증은 아니다. 범용 선택지 `말을 건네본다/조용히 지켜본다/도움을 건넨다`는 T5~7에 남았다. 자동 13/15(V2·V6 표본), 정본 감사 실제/회색 0/0, 대사 마커 14/15(93.3%). 다른 시드나 신규 블록의 라이브 응답까지 안정적이라 단정하지 않는다.
- 후속 검토에서 발견한 경계도 반영했다. 기존 trust 분기만 보면 `OBSERVE/SEARCH/STEAL`, 비질문 `INVESTIGATE`로 얻은 사실도 trust가 높을 때 `direct`로 저장돼 재질문에서 과거 NPC 발언으로 오분류된다. 서버의 출처 기록과 서술 프롬프트가 **하나의 공개 방식 분류기**를 사용하게 해, 비대화 행동·관찰 동사는 `observe`, 명시적인 질문형 `INVESTIGATE`는 NPC 공개 방식으로 일치시켰다. 단서 발견 자체는 유지한다. 현재 판정이 FAIL이거나 유효 태도가 HOSTILE이면 NPC 확답은 강제하지 않되 발견 정본을 **플레이어 지식**으로 계속 전달한다. 해당 fact를 모르는 NPC에게 묻는 경우도 정본은 서술에 보존하되, NPC가 자기 지식처럼 확언하지 않고 모름/인계로 반응한다. 서버 출처·프롬프트의 교차 분기와 실패·적대·비보유 NPC의 회귀 테스트는 통과했다. 이 보강 이후 동일 자극 실런은 아직 없고, 서버가 의도한 `direct` 방식이 실제 생성 대사에도 구현됐는지 자동 검증하는 장치는 아니다.
- 최종 로컬 회귀: 서버 Jest 205 스위트·2781 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip), 변경 파일 ESLint PASS, 서버 빌드 PASS, 콘텐츠 감사 ERROR/WARN 0(INFO 14), root/server `git diff --check` PASS. 로컬 launchd 재시작 뒤 3턴 스모크 PASS. `GET /v1/version`의 `74e3974`는 미커밋 빌드의 내용 해시가 아니라 시작 기준 HEAD이므로, 스모크의 uptime과 재시작 시각을 함께 확인했다. 이번 변경은 root/server에 **로컬 미커밋·미푸시** 상태이고 client 추적 파일은 수정하지 않았다.

## 16. 2026-09-26 다음 두 블록 착수 — 후속 선택과 장면 출처

- 이전 §15 변경은 root `557f504`, server `74e1f2c`로 각각 커밋·푸시했다. 그 기준의 서버 3턴 스모크는 통과했다. 아래는 그 다음 작업이며, §15의 당시 미커밋 상태 설명을 소급해 바꾸지 않는다. 클라이언트 추적 코드는 이번에도 변경하지 않았다.
- **블록 1 / P3·N-05/06/07/17 — 부분 구현:** 서버가 이번 턴에 실제 발견하고 **본문 공개 슬롯에도 실은** fact의 저작 `nextHint`를 Nano의 선택지 생성에 별도 전달한다. 한 턴에 두 fact가 발견돼도 슬롯 밖 fact의 힌트는 선택지에 선공개하지 않는다. 최근 3턴 이월은 같은 NPC가 공개했거나 플레이어가 현장에서 확인했고 해당 NPC가 아는 fact 중, 현재 입력과 주제가 매칭되는 것에만 제한한다. 이 힌트는 NPC의 과거 발언을 단정하거나 새 fact를 여는 판정이 아니다. 판정 후 발견 fact를 Nano의 선계산 `availableFacts`에서 제외하고 최신 `questState`를 반영해, 이미 얻은 단서를 새 정보처럼 재선택하는 위험을 줄인다. 이름 공개 조건에 따라 힌트도 정리한다. 동일 입력·다른 NPC·오래된 출처·미발견 fact·다중 발견·관찰 출처·저작 키워드의 다른 표현을 단위 테스트로 고정했다.
- **블록 2 / P1·N-10/11 — 출처 계약과 전환 지시:** 회계사 동적 사건에서 악사 이벤트 선택지가 최종 후보로 섞이지 않는 기존 서버 경로를 실제 팩 기반 회귀 테스트로 고정했다. 저작 이벤트가 장소 내부의 다른 하위 무대로 옮길 때에만 `subsceneTransition`을 명시하고, `EVT_GUARD_INT_3`의 경비대 입구→외곽 술집에 적용했다. 최근 턴의 실제 `eventId`를 프롬프트에 전달해 같은 저작 이벤트의 재방문과 새 사건 전환을 구분하고, 새 전환이면 이동 연결 문장을 요구한다. 첫 등장·일반 이벤트·`FREE_` 턴은 임의의 장소 변경으로 취급하지 않는다. 이는 **프롬프트·출처 경계**의 구현이지, 레닉 첫 조우의 실모델 본문 통과를 뜻하지 않는다.
- 그레이마르 에드릭 연속 7턴 `8ba9caa7-ff50-4f5a-ac99-9b36e64d76b9`(`playtest-reports/narrative_quality_20260926_edric_repeat_block12.json`)의 T4 동적 사건 `SIT_ACTIVITY_NPC_EDRIC_VEIL_1`은 서버 즉시 선택지와 최종 `llmChoices`에 악사 항목이 없었다. 이는 N-10의 **한 실런 표본**이다. 그러나 T5~7 장부 설명은 다시 비슷한 질문으로 돌아갔고 생성 본문에는 목록형 표현·근거가 불분명한 종이 묘사가 있었다. 자동 14/15를 대화 진전 통과로 읽지 않는다.
- 극야해안 이렌 연속 7턴 재실험 `7d113c08-db92-414e-bd9c-e00d8e8b97ea`(`...iren_repeat_block12_retest.json`)에서는 T4의 첫 꿈이 추락 전날이며 사흘간 깨어나지 못했다는 사실이 읽혔다. 그러나 T5~7은 이미 확인한 시점을 다시 기억하지 못하는 듯 답했고, 일부 후속 선택지는 다시 범용 질문이었다. 첫 실험 `41893b39-4b7c-4f4d-b1e4-c470cc5985c5`(`...iren_repeat_block12.json`)도 같은 잔여 문제를 보였다. **블록 1은 선택지 맥락 배선까지만 착수**, N-06의 본문 문답 안정성과 N-05의 동일 분모 선택지 개선은 미통과다. N-07의 NPC별 반응 기능 다양화도 아직 미구현이다.
- 다음 관문은 (1) 이렌의 `observe`/이벤트 출처와 직접 공개 출처를 분리해 저장 프롬프트·완성 본문의 충돌 원인을 계측하고, (2) 저작 `nextHint`가 실제 선택지/클릭 판정까지 이어졌는지 같은 선택지 표면·분모로 비교하며, (3) `EVT_GUARD_INT_3`의 실제 첫 조우에서 입구→술집 연결이 보이는지 확인하는 것이다. 자동 점수나 한 번의 무누출 표본으로 P1/P3 완료를 선언하지 않는다.
- 검토 중 한 턴 다중 발견에서는 마지막 fact가 본문 공개 슬롯에 없을 수 있다는 누출 경로를 발견해, 힌트 출처를 `questRevealThisTurn.factId`에 일치시키고 회귀 테스트를 추가했다. 최종 로컬 확인은 서버 Jest 207 스위트·2790 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip), 서버 빌드 PASS, 변경 파일 ESLint 오류 0건, 그레이마르 콘텐츠 감사 ERROR/WARN 0(INFO 14), root/server `git diff --check` PASS다. 7턴 실런들은 이 마지막 안전 보강 전 실행이라 보강판의 실모델 선택지 통과 증거로 쓰지 않는다.

## 17. 2026-09-26 블록 1·2 품질 폐쇄 검토 — 핵심 경로 고정, 상위 P1/P3와 구분

- 작업 시작 기준은 root `e1e445c`, server `64747c8`이다. 아래 코드는 이 절 작성 시 **로컬 미커밋·미푸시**다. `/v1/version`의 `64747c8`은 미커밋 빌드의 내용 해시가 아니므로, 로컬 빌드·재시작 시각·실런 결과로만 반영 여부를 확인한다. client 추적 코드는 수정하지 않았다.
- **블록 1의 구체 경로:** 새 사실 `FACT_SS_FIRST_DREAM`이 실제 공개되면 이렌에게 다음 사실을 물을 수 있는 저작 선택지를 서버·최종 선택지 양쪽에 같은 payload로 넣는다. 그 선택지는 `sourceEventId=EVT_SS_INN_SAME_WORDS`를 가져 클릭 뒤 엉뚱한 여관 사건으로 빠지지 않는다. 사건의 문장과 사실 정본도 `눈이 아직 바다를 보고 있다`로 일치시켰다. 이렌의 이미 공개한 첫 꿈을 다시 물을 때는 날짜 표현의 단순 존재가 아니라 부정·수면 역전을 검사해, 모순된 NPC 대사만 정본 답변으로 교정한다. 에드릭이 `FACT_INSIDE_JOB`을 아직 공개할 수 없는 실패 턴에는 결론을 누설하지 않고 저작된 `경비대 지구 / 출입 기록` 경로를 NPC 대사와 `MOVE_LOCATION` 선택지로 제공한다. 성공·부분 성공의 정상 fact 공개는 그대로 둔다.
- **블록 1 실측:** 이렌 10턴 `16fa2800-f8a6-46aa-9f79-206cca9efc51`(`playtest-reports/narrative_quality_20260926_iren_bound_click_final.json`)은 T3 첫 꿈 공개→T4 `fact_followup_4` 클릭→정확히 `EVT_SS_INN_SAME_WORDS`·이렌·정본 인용문·장면 고유 선택지로 이어졌다. 자동 14/15(V2 조우 표본), 대사 마커 25/25. 에드릭 5턴 `ef2ac86c-6a0e-40c6-b671-893ecb6e61ce`은 클릭 성공으로 새 fact가 열렸다. 후속 실패 분기의 기존 실런 `8c5c27cc-e289-4c19-b845-c9301f02d0d5`는 모호한 거절을 재현해 수정 근거가 됐다. 수정 후 4턴 표적 런 3회(`3057b9ff-f886-41ee-ac3a-988e5e0c1dae`, `66ab4738-ac39-46ea-8408-53a6ff7466d1`, `03351a88-4b33-47bd-a5da-d67a894c6680`)는 각각 SUCCESS/PARTIAL/PARTIAL이었고, 추가 강경 추궁 `2792023b-c515-4fee-844e-e6788faa2251`도 SUCCESS라 **실패 분기의 새 대사·선택지를 라이브로 재검증하지 못했다**. 해당 분기는 context-builder·후처리·선택지 코어 단위 테스트로 고정했다. 상위 P3의 범용 선택지 빈도는 P0와 동일 분모·표면 비교가 아직 없으므로, 블록 구현을 P3 전체 완료로 올리지 않는다.
- **블록 2의 구체 경로:** 플레이어가 실제로 외곽 술집에서 행동할 때만 저작 하위 무대를 우선하고, 술집이 어디인지 묻거나 방문을 부정한 말은 이동 신호로 쓰지 않는다. 첫 진입은 같은 방문의 전체 사건 이력으로 판정해 중간 `FREE_` 턴 뒤 재등장을 새 입장으로 오인하지 않는다. 본문에 두 장소 이름만 있을 뿐 이동이 없으면 저작 동선 문장을 앞에 보강한다. 최종 Nano 선택지가 다른 NPC로 향하면 첫 하위 무대에서는 사건 출처가 달린 서버 선택지를 보존한다. 콘텐츠 감사는 directed 사건의 gates·affordances 배열과 이동 문장·앵커를 검증한다.
- **블록 2 실측:** 그레이마르 5턴 `d72c762a-06a7-4821-9bdb-5ab74d262af9`(`playtest-reports/narrative_quality_20260926_guard_transition_postreview.json`)의 T3는 `EVT_GUARD_INT_3`·`NPC_RENNICK`으로 가며 첫 문장에 경비대 길목→외곽 술집 이동이 있었다. 노출 선택지 3개는 모두 해당 사건 저작 선택지였고 T4 클릭은 같은 사건·NPC로 이어졌지만 입장 문장을 반복하지 않았다. 품질 감사 실제/회색 0/0, 마커 10/10. 위치 문의·부정문·사건→FREE→같은 사건, 장소명만 등장하는 본문은 고정 회귀 테스트가 통과했다. 이 범위의 N-10/11 장면 출처·첫 전환 관문은 닫는다. P1의 다른 자유 행동/전투 사례와 실제 브라우저 UI 전체는 별도다.
- 최종 회귀: 서버 빌드 PASS, Jest 211 스위트·2814 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip), 변경 파일 ESLint 오류 0건(기존 테스트 `any` 경고 13건), Python 신규 콘텐츠 계약 테스트 7건 PASS, 두 팩 콘텐츠 감사 ERROR/WARN 0, root/server `git diff --check` PASS, 최종 로컬 서버 3턴 스모크 PASS. 별도 어휘 감사에서는 위 이렌 10턴의 극야해안 `햇빛` 1건, 에드릭 성공 5턴의 금지 화폐 `닢` 1건이 남았다. 둘 다 이번 선택지·장면 출처 계약의 실패로 은폐하지 않으며 P2/P6 어휘 품질로 추적한다.

## 18. 2026-09-26 잔여 어휘·서술·결말 블록 — 저작 계약 개선, 상위 관문은 계속 열림

- §17의 블록 1·2 변경은 root `fbd057e`, server `a61bbab`로 각각 커밋·푸시했고, 서버 재시작 및 3턴 스모크를 통과했다. 이번 추가 블록은 이 두 커밋을 시작 고정점으로 삼는다. client 추적 코드는 변경하지 않았다.
- **P2/N-08 저작 화폐:** 그레이마르·극야해안의 노출 선택지·장면·NPC 화폐 표현을 `골드` 또는 금액을 주장하지 않는 문구로 고쳤다. 실제 차감이 없는 악사 `slm_atm3_coin` 선택지는 기존 ID를 보존하고 지불 행동을 노래 요청으로 바꿨다. 콘텐츠 감사에 비정본 화폐 단위(`금화/은화/은전/동전/닢`)의 노출 문구 계약을 더했다. `keywords`는 플레이어 표현 인식을 위해 제외하고, 사건·NPC·선택지·결말·시나리오·사실·장소·아이템·상점 문구를 검사한다. 저작 원천의 재발 방지이지 모델이 생성하는 모든 화폐 어휘의 차단 증명은 아니다.
- **P6/N-09 및 극야 광원:** 감각 예문과 매 턴 1~2개 사용 강제를 제거하고, 행동·반응·발견에 연결될 때만 짧게 쓰도록 했다. 극야해안의 체류 턴에도 시간대 불변 조건을 압축 전달한다. 검토에서 `NIGHT`가 “완전한 어둠”만 남고 허용 광원을 잃는 회귀를 발견해 오로라·고래기름 등불·인광의 출처 문장도 유지하고 단위 테스트로 고정했다. 이 마지막 밤 수정 뒤 동일 자극 실런은 아직 없다.
- **P5/N-16 결말:** `PROFIT_FROM_CHAOS`의 고정 후일담이 조기 안전망과 3막 완주를 혼동하지 않도록 완료 단계 3을 결말 입력에 전달해 분기한다. 완주 문단은 실제 거래의 이익과 신뢰 상실을, 조기 문단은 거래 결과의 미확정을 말한다. `ALLY_GUARD/STABLE`의 미지급 명예 휘장·실제 작전 성공 단정도 제거했다. 분기와 전달은 단위 테스트로 확인했으나 이 노선들의 현행 명시 완주 실런은 없다.
- **극야해안 10턴 실런:** `14feaa4e-bfd6-4f93-a0c4-ffe8738d0107` (`playtest-reports/narrative_quality_20260926_phase_currency_postfix.json`)은 자동 V0~V14 15/15, 정본 감사 실제/회색 0/0, 마커 23/25(92%)였다. 과거 표본의 `햇빛`과 비정본 화폐는 이 표본에서 재발하지 않았다. 그러나 T6에서 “세 사람이 외운 문장”을 직접 물었을 때 이렌이 문장을 밝히지 않고 되묻거나 미뤘고, 무명 화자 마커가 섞였다. T9의 “셈 빠른 중개인 중개인이”처럼 지칭 중복도 있었다. P3 문답 진전과 P6 인물 표면을 완료 처리하지 않는다.
- **그레이마르 10턴 실런:** `175d21a4-1cda-4cfb-a314-c7a51f491223` (`playtest-reports/narrative_quality_20260926_graymar_currency_postfix.json`)은 자동 14/15(V9 반복 어휘 실패), 정본 감사 실제/회색 0/0, 마커 27/27이었다. 금지 화폐 단위는 없었지만 에드릭 구간의 `종이` 반복과 마이렐 구간의 `그대` 반복이 V9 임계치를 넘었다. T10은 이미 장면 화자가 있는데도 익명 인물 대사 둘을 추가했다. 감사 0건은 이 정성 결함의 해소를 뜻하지 않는다.
- 최종 로컬 회귀는 서버 Jest 211 스위트·2818 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip), 서버 빌드 PASS, 변경 서버 파일 ESLint 오류 0건, Python 콘텐츠 계약 9건 PASS, 전 팩 콘텐츠 감사 ERROR/WARN 0, root/server `git diff --check` PASS다. 두 실런은 현재 서버의 미커밋 빌드를 재시작해 수행했으며 `/v1/version`의 `a61bbab`은 빌드 내용 해시가 아니다. 각 런의 비교군 지문도 다르므로 두 점수를 동일 조건 A/B 개선율로 해석하지 않는다.
- **미닫힘 관문:** (1) P3은 이렌의 직접 문장 질문이 저작 사실/사건으로 이어지는지와 대사 화자 일관성을 별도 표적 런으로 확인한다. (2) P6은 같은 NPC 3턴 이상에서 반복 지칭·감각 도입의 사람 정독 판정을 확보한다. (3) P5는 거래·경비대 조기 안전망과 3막 완료 각각을 명시 피날레로 완주해 단계별 문단·지급을 대조한다. (4) P7은 두 팩의 비교 가능한 10~15턴 누적 표본과 인증된 브라우저 스트리밍·완료 화면이 필요하다. 이 조건 전에는 전체 계획/출시를 닫지 않는다.
- **커밋·반영:** 이 블록의 구현은 server `167b7f9`, root `3fcbff8`로 `origin/main`에 푸시했다. 서버 `pnpm build` 후 launchd를 재시작해 `/v1/version`이 `167b7f9`·`startedAt=2026-09-26T09:35:05.339Z`인 것을 확인했다. 최종 3턴 스모크는 초대 게이트 ON/무코드 가입 거절, 테스트 가입·런 생성·3턴까지 PASS였다. 이 최종 스모크는 P3/P5/P6의 위 정성 관문을 대신하지 않는다.

## 19. 2026-09-26 직접 문장 질문 회귀 — 표적 경로 통과, 잔여 게이트 분리

- §18의 극야해안 T6 질문은 `그 밤 꿈속에서 세 사람이 외운 문장을 다시 한번 묻는다. 그 문장이 도대체 무엇이었는지 궁금하다.`였다. `isQuestionInput`이 마지막 문장의 서술형 끝만 확인해 질문 행동을 놓쳤고, 사실/NPC 키워드에는 `외운 문장`이 없었다. 정보 목적어에 바로 이어지는 `묻는다`만 추가 인식하고, 매장 행동을 묻는 질문으로 오인하지 않는 세 회귀 사례를 고정했다. `FACT_SS_SAME_WORDS`와 이렌·헬룬의 저작 키워드를 동기화하고, 이렌의 기존 공개 사실 재확인 문장을 저작했다. 검토 중 제안됐던 프롤로그 공개 사실의 별도 허가 경로는 정상 발견 턴에서 우회되어 실제 증거가 없으므로 도입하지 않았다.
- 수정 빌드를 재시작한 표적 4턴 `6fecfaef-9037-4c42-901a-bcb6fd22257a` (`playtest-reports/narrative_quality_20260926_question_detector_postfix.json`)의 T3는 같은 플레이어 문장에 `QUEST`·`NPC`가 발동했고, 장면 화자 헬룬이 `마지막 말은 모두 같았소. '눈이 아직 바다를 보고 있다'였소.`라고 직접 답했다. 따라서 이 **직접 문장 질문 경로**는 통과했다. 자동 12/15는 짧은 표본의 조우·기억·판정 게이트 미달로 전체 품질 통과가 아니다.
- 정본 품질 감사에서 같은 런 수락 턴에 근거 없는 `동전 하나를 탁자 가장자리에 올려둔다`가 실제 위반 1건으로 확인됐다. 저작 화폐 정리는 생성 서술의 행동·차감 환각을 막지 못한다. 화폐 낱말만 `골드`로 바꾸면 미발생 지불을 정본화하므로 이 회차에는 그런 치환을 하지 않았다. 이는 P2/N-08에 남기고, 수락·무지불 턴의 지불 서술 금지 및 원장 일치 검증으로 다룬다.
- 우선 해당 별모래 수락 지시에는 **수락만 확정됐고 돈·물건을 주고받거나 약속한 보수를 받은 것은 아니라는 조건**을 추가했다. 재시작 후 1턴 `168c3326-6c93-4f6d-9854-ab7750672833` (`playtest-reports/narrative_quality_20260926_accept_no_payment_postfix.json`)에서는 지불 장면 없이 수락·이렌의 반응·여관 이탈이 이어졌고, 정본 감사 실제/회색 0/0·마커 1/1이었다. 한 표본의 프롬프트 완화일 뿐 모든 수락/자유 행동의 지불 환각 차단이나 포인트 원장 일치 관문을 통과한 것은 아니다.
- 회귀: 서버 Jest 211 스위트·2822 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip), 빌드·변경 파일 ESLint PASS, Python 콘텐츠 단위 9건 PASS, 전 팩 콘텐츠 감사 ERROR/WARN 0. 별칭 중복/무명 화자(P6), P5 단계별 명시 피날레 완주, 두 팩 누적 정성 표본과 인증 브라우저 화면(P7)은 여전히 열린 관문이다. 이 경로 수정 하나로 P3 전체나 마스터 계획을 닫지 않는다.
- 반영: 질문 판별 server `ba77546`, 저작 사실·검증 root `98acd55`, 수락 무지불 지시 root `ac6a2dd`를 각각 `origin/main`에 푸시했다. 최종 서버 빌드·launchd 재시작 뒤 `/v1/version`은 `ba77546`·`startedAt=2026-09-26T09:54:05.600Z`였고 초대 게이트 ON/무코드 가입 거절·회원가입·런 생성·3턴 스모크가 PASS였다. 이후 root의 지시 추가로 다시 서버를 재시작해 1턴 표적 런을 확인했으므로 실행 중 콘텐츠도 반영됐다.
- P6의 §18 `셈 빠른 중개인 중개인이`는 완전 별칭 뒤에 동일 NPC의 짧은 직책 별칭이 재삽입된 형태였다. 후처리는 **전체 별칭이 짧은 별칭으로 끝나는 경우**의 인접 중복만 제거하고, 정상 문장·NPC 마커는 그대로 둔다. 실제 서비스 메서드 단위 회귀 2건을 추가했다. 이 형태가 다시 출력되지 않은 10턴을 수정의 실증으로 오인하지 않는다.
- 수정 빌드의 극야해안 채티 10턴 `a5a283ac-78b5-4418-8933-7dc9a34014b7` (`playtest-reports/narrative_quality_20260926_alias_suffix_postfix.json`)은 자동 15/15, 정본 품질 감사 실제/회색 0/0·마커 23/23, 같은 NPC 최장 3턴이었다. T5~7 이렌에게 첫 꿈→공통 문장 후속 질문이 이어져 T7의 정본 문구가 직접 나왔다. 그러나 그 답변에서 원래 **입으로 말한** 문장이 `같은 쪽지를 접어 두고 갔다`로 바뀐 사실 환각이 확인됐다. `EVT_SS_INN_SAME_WORDS`의 저작 장면은 이 차이를 명시하도록 좁혔다. 새 장면 프레임 뒤 동일 사실의 라이브 재검증은 별도로 필요하다. T5의 생선값 잡담과 런 전체의 `이렌` 반복도 자동 PASS가 잡지 못하는 정성 과제로 남는다.
- 저작 장면 수정 후 극야해안 채티 7턴 `537c675f-9678-4119-8c93-25873e5b1c8b` (`playtest-reports/narrative_quality_20260926_spoken_phrase_postfix.json`)에서 동일 `EVT_SS_INN_SAME_WORDS`가 다시 발동했다. T6 이렌은 `잠들기 전에 입으로 남기는 말이에요. 쪽지도 아니고`라고 구분하고 정본 문구를 직접 말했으므로 **이 사건의 입말/쪽지 정합 회귀는 한 실런에서 통과**했다. 자동 14/15(V2 짧은 조우 표본), 당시 정본 감사 실제/회색 0/0·마커 17/18(94.4%)였다. 마커 수치의 원인은 아래 §20에서 빈 대사 줄에 따른 감사기 오탐으로 확인됐다. 지속된 `이렌` 반복 때문에 P6 전체는 닫지 않는다.
- 별칭 후처리 server `ead6e65`와 사건 저작 root `87df801`은 `origin/main`에 푸시했다. 서버 최종 빌드·launchd 재시작 후 `/v1/version`은 `ead6e65`·`startedAt=2026-09-26T10:06:33.521Z`, 초대 게이트 ON/무코드 가입 거절·회원가입·런 생성·3턴 스모크 PASS다. 전체 Jest 212 스위트·2824 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip), 변경 파일 ESLint 및 극야해안 콘텐츠 감사 ERROR/WARN 0이다. P5 다른 노선의 조기/3막 명시 완주와 P7 인증 브라우저 UI는 아직 실행 증거가 없다.

## 20. 2026-09-26 잔여 표면 회귀 — 빈 대사와 생성 선택지 화폐

- 시작 기준은 root `2e67b8d`, server `ead6e65`, client `415d102`이다. 추적 파일은 모두 깨끗했고 이전 JEV 자료·주사위 시안 미추적 파일은 이번 범위 밖으로 보존한다.
- P2/P6 대사 마커: §19의 7턴 원문 DB에는 비어 있지 않은 모든 대사에 `@` 마커가 있었다. 수락 T1의 `@[이렌|NPC_SS_IREN] ""` 빈 줄 뒤 다음 대사의 따옴표까지 감사기 정규식이 줄을 넘어 잘못 묶어 17/18을 만들었다. 실제 문제는 **빈 화자 줄 노출**과 **감사 분모 오탐**이다. 서버 완성 본문 정리 경계에서 내용 없는 화자 줄만 제거하고, 정본 감사는 대사 매칭을 한 줄에 제한한다. 기존 저장 런 재계측은 실제/회색 0/0·마커 18/18로 바로잡혔다. 이 조치는 중간 스트림 조각의 실제 브라우저 렌더를 검증한 것은 아니다.
- P2/N-08 선택지: 별모래 10턴 `a5a283ac-78b5-4418-8933-7dc9a34014b7`의 생성 선택지 `탁자 위 동전들을 살핀다`는 정본 화폐가 없는 장면에 없는 물건을 만든다. 생성(`nano_`/`choice_`) 라벨의 비정본 화폐만 제거하고 정상·저작 선택지는 유지한다. 유효 행동이 2개 미만이면 기존 계약대로 서버 기본 선택지로 돌아간다. 플레이어 자유 입력은 세계 밖 판정으로 다시 분류하지 않는다. 단, 이 폴백에서 유효했던 생성 질문 하나가 함께 사라질 수 있어 장면 고유 선택지 유지율은 후속 계측 대상이다.
- 수정 빌드의 극야해안 채티 10턴 `d8efe865-543a-4060-acd2-6b4989d07f36` (`playtest-reports/narrative_quality_20260926_empty_marker_currency_choice_postfix.json`)은 자동 15/15, 정본 감사 실제/회색 0/0·마커 23/23이었다. 수락 턴에 빈 대사 마커가 없고 노출 선택지에 금지 화폐 단위도 없었다. 그러나 이 런에서 문제의 `동전` 생성 후보 자체는 재발하지 않았으므로, 선택지 필터의 검증 근거는 표적 입력을 사용하는 실패→통과 단위 테스트다. 전체 서술 품질이나 출시 조건 완료로 확대하지 않는다.
- 로컬 회귀: 서버 Jest 212 스위트·2827 테스트·스냅샷 17건 PASS(기존 1스위트·2테스트 skip), 서버 빌드·변경 파일 ESLint PASS, Python 정본 감사 단위 10건 PASS, 전 팩 콘텐츠 감사 ERROR/WARN 0. 남은 큰 관문은 P5 다른 노선의 조기/3막 명시 완주, P6 연속 화자 정독·스트림 경계, P7 인증 브라우저 완료 화면이다.
- 커밋·반영: server `b854429`, root `a473406`을 `origin/main`에 푸시했다. 서버를 다시 빌드·launchd 재시작한 뒤 `/v1/version` `b854429`·`startedAt=2026-09-26T10:25:47.270Z`를 확인했고, 초대 게이트 ON/무코드 가입 거절·회원가입·런 생성·3턴 스모크 PASS였다. 독립 검토는 명세 위반 0건, 규약 위반 0건; 금지 화폐 목록이 프롬프트·감사기·선택지 필터에 분산된 유지보수 주의점 1건을 남겼다. 이번 변경은 클라이언트 추적 코드를 수정하지 않았다.
